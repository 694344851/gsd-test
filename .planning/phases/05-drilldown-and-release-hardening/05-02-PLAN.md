---
phase: 05-drilldown-and-release-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - frontend/src/lib/manager-drilldown-api.ts
  - frontend/src/app/manager-drilldown-page.tsx
  - frontend/src/app/__tests__/manager-drilldown-page.test.tsx
  - frontend/src/components/export-cases-button.tsx
  - frontend/src/lib/export-api.ts
  - src/api/http_dashboard_server.py
  - src/api/http_dashboard_drilldown.py
  - src/api/http_dashboard_export.py
  - src/api/analytics/drilldown_smoke.py
  - src/api/analytics/export_smoke.py
  - src/domain/analytics/query_service.py
  - sql/tests/oper_01_problem_drilldown.sql
  - sql/tests/oper_02_case_export.sql
  - sql/tests/run-phase-01.sh
autonomous: true
requirements:
  - OPER-02
  - OPER-01
must_haves:
  truths:
    - "导出必须复用与明细页相同的服务端筛选语义，不能让下载结果和屏幕看到的病例集合脱节。"
    - "05-02 必须把 05-01 的 typed drilldown seam 接到真实服务端查询，不再停留在前端注入结果。"
    - "CSV 生成应在后端完成，前端只负责发起下载和呈现状态。"
    - "导出能力要基于当前 drilldown 筛选条件，而不是全量病例导出。"
  artifacts:
    - path: "src/api/http_dashboard_drilldown.py"
      provides: "HTTP handler for real drilldown data"
      contains: "handle_problem_drilldown"
    - path: "src/api/http_dashboard_export.py"
      provides: "CSV export handler for filtered problem cases"
      contains: "handle_problem_case_export"
    - path: "src/api/analytics/export_smoke.py"
      provides: "Behavioral smoke for CSV headers, filename, and representative rows"
      contains: "Content-Disposition"
  key_links:
    - from: "frontend/src/lib/manager-drilldown-api.ts"
      to: "src/api/http_dashboard_drilldown.py"
      via: "frontend drilldown shell binds its typed seam to the real manager drilldown endpoint"
      pattern: "/api/problem-drilldown"
    - from: "src/api/http_dashboard_export.py"
      to: "src/domain/analytics/query_service.py"
      via: "export handler reuses the canonical drilldown case-query path"
      pattern: "get_problem_case_rows"
    - from: "src/api/analytics/export_smoke.py"
      to: "src/api/http_dashboard_export.py"
      via: "behavioral smoke asserts CSV serialization, content type, and deterministic filename contract"
      pattern: "text/csv|Content-Disposition"
---

<objective>
交付问题病例导出和下载能力，使管理者能够把当前筛选下的病例明细导出用于人工复核，同时保持与屏幕明细的语义一致。

Purpose: 完成 OPER-02，并把 05-01 的前端 drilldown 壳接上真实服务端明细查询，再把导出做成与明细共享语义的 CSV 能力。
Output: real drilldown endpoint、frontend backend binding、export trigger UI、typed export client、CSV export handler、SQL/smoke verification。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-drilldown-and-release-hardening/05-RESEARCH.md
@.planning/phases/05-drilldown-and-release-hardening/05-VALIDATION.md
@.planning/phases/05-drilldown-and-release-hardening/05-01-PLAN.md
@frontend/src/app/manager-drilldown-page.tsx
@src/api/http_dashboard_server.py
@src/domain/analytics/query_service.py
@门诊诊鉴首页-(全栈).md

<interfaces>
Plan 05-02 must expose an export path shaped like:
```http
GET /api/problem-cases/export?dimension=department&dimension_value=内科&range_key=last_30_days
Accept: text/csv
```

And a frontend trigger boundary shaped like:
```ts
export interface ProblemCaseExportRequest extends DrilldownRequest {
  filename_hint?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind the drilldown shell to a real backend detail endpoint</name>
  <read_first>
    .planning/phases/05-drilldown-and-release-hardening/05-RESEARCH.md
    .planning/phases/05-drilldown-and-release-hardening/05-01-PLAN.md
    frontend/src/app/manager-drilldown-page.tsx
    frontend/src/lib/manager-drilldown-api.ts
    src/domain/analytics/query_service.py
  </read_first>
  <files>frontend/src/lib/manager-drilldown-api.ts, frontend/src/app/manager-drilldown-page.tsx, frontend/src/app/__tests__/manager-drilldown-page.test.tsx, src/api/http_dashboard_server.py, src/api/http_dashboard_drilldown.py, src/api/analytics/drilldown_smoke.py, src/domain/analytics/query_service.py, sql/tests/oper_01_problem_drilldown.sql, sql/tests/run-phase-01.sh</files>
  <acceptance_criteria>
    - `src/api/http_dashboard_drilldown.py` exposes a GET handler such as `handle_problem_drilldown`.
    - `src/api/http_dashboard_server.py` dispatches the new manager drilldown route without disturbing existing endpoints.
    - `src/domain/analytics/query_service.py` provides a canonical `get_problem_case_rows(...)`-style helper reused by both detail and export flows.
    - `frontend/src/lib/manager-drilldown-api.ts` binds the 05-01 typed client seam to the real drilldown endpoint.
    - `src/api/analytics/drilldown_smoke.py` exits 0 after exercising the handler/query path.
    - `sql/tests/oper_01_problem_drilldown.sql` proves filtered drilldown rows are returned with the expected problem flags and ordering.
  </acceptance_criteria>
  <action>Implement the real server-side drilldown path and bind it into the UI seam established in 05-01. Add `src/api/http_dashboard_drilldown.py` to parse manager drilldown query parameters, validate dimension/filter inputs, and delegate to the analytics query layer. Extend `src/domain/analytics/query_service.py` with a canonical helper such as `get_problem_case_rows(...)` that resolves case-level rows from existing analytics and persisted Phase 4 evaluation data without redefining metric formulas in Python. Update `src/api/http_dashboard_server.py` to dispatch the new GET route, bind `frontend/src/lib/manager-drilldown-api.ts` to this endpoint, and keep `frontend/src/app/manager-drilldown-page.tsx` aligned with the real response shape. Add `sql/tests/oper_01_problem_drilldown.sql` and `src/api/analytics/drilldown_smoke.py` so the handler/query path is proven by both SQL semantics and a direct-run HTTP-level smoke script.</action>
  <verify>
    <automated>python3 -m py_compile src/api/http_dashboard_server.py src/api/http_dashboard_drilldown.py src/api/analytics/drilldown_smoke.py src/domain/analytics/query_service.py && python3 src/api/analytics/drilldown_smoke.py && export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh oper_01_problem_drilldown && cd frontend && npm test -- --run src/app/__tests__/manager-drilldown-page.test.tsx</automated>
  </verify>
  <done>管理端下钻已经从前端壳升级为真实服务端病例明细链路，export 可以直接复用这条语义路径。</done>
</task>

<task type="auto">
  <name>Task 2: Add CSV export endpoint and verify real download behavior</name>
  <read_first>
    .planning/phases/05-drilldown-and-release-hardening/05-RESEARCH.md
    .planning/phases/05-drilldown-and-release-hardening/05-01-PLAN.md
    .planning/phases/05-drilldown-and-release-hardening/05-02-PLAN.md
    frontend/src/app/manager-drilldown-page.tsx
    frontend/src/lib/manager-drilldown-api.ts
    src/api/http_dashboard_server.py
    src/domain/analytics/query_service.py
  </read_first>
  <files>frontend/src/app/manager-drilldown-page.tsx, frontend/src/app/__tests__/manager-drilldown-page.test.tsx, frontend/src/components/export-cases-button.tsx, frontend/src/lib/export-api.ts, src/api/http_dashboard_server.py, src/api/http_dashboard_export.py, src/api/analytics/export_smoke.py, src/domain/analytics/query_service.py, sql/tests/oper_02_case_export.sql, sql/tests/run-phase-01.sh</files>
  <acceptance_criteria>
    - `frontend/src/components/export-cases-button.tsx` renders an export trigger bound to the current drilldown scope.
    - `frontend/src/lib/export-api.ts` builds the export request from the current typed drilldown filters instead of scraping rendered rows.
    - `src/api/http_dashboard_export.py` exposes a CSV handler such as `handle_problem_case_export`.
    - Export uses the same canonical case-query helper as drilldown rather than a separate data path.
    - The HTTP response includes CSV content and a deterministic filename contract suitable for manager review usage.
    - `src/api/analytics/export_smoke.py` proves `Content-Type`, `Content-Disposition`, filename, and representative CSV rows.
    - `sql/tests/oper_02_case_export.sql` proves the exported row set matches the intended filtered problem-case scope.
  </acceptance_criteria>
  <action>Implement the export path on top of the canonical drilldown query from Task 1. Add `frontend/src/lib/export-api.ts` and `frontend/src/components/export-cases-button.tsx` so the manager drilldown screen can request a download using the exact same active filters and dimension selection used for on-screen detail. Add `src/api/http_dashboard_export.py` to parse export parameters, call the canonical case-query helper, and serialize CSV using the Python standard library. Update `src/api/http_dashboard_server.py` to dispatch the export route, keep `src/domain/analytics/query_service.py` as the single source of filtered case semantics, add `sql/tests/oper_02_case_export.sql` for row-set correctness, and add `src/api/analytics/export_smoke.py` as a direct-run behavioral smoke that asserts CSV headers, deterministic filename, and representative serialized rows.</action>
  <verify>
    <automated>python3 -m py_compile src/api/http_dashboard_server.py src/api/http_dashboard_export.py src/api/analytics/export_smoke.py src/domain/analytics/query_service.py && python3 src/api/analytics/export_smoke.py && export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh oper_02_case_export && cd frontend && npm test -- --run src/app/__tests__/manager-drilldown-page.test.tsx</automated>
  </verify>
  <done>病例导出已经和管理端明细查询共享同一后端语义，并且 CSV 行为本身有了真实 smoke 覆盖。</done>
</task>

</tasks>

<verification>
执行 `python3 -m py_compile src/api/http_dashboard_server.py src/api/http_dashboard_drilldown.py src/api/http_dashboard_export.py src/api/analytics/drilldown_smoke.py src/api/analytics/export_smoke.py src/domain/analytics/query_service.py`，再执行 `python3 src/api/analytics/drilldown_smoke.py` 和 `python3 src/api/analytics/export_smoke.py`，再执行 `export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh oper_01_problem_drilldown oper_02_case_export`，最后执行 `cd frontend && npm test -- --run src/app/__tests__/manager-drilldown-page.test.tsx`。
</verification>

<success_criteria>
`OPER-01` 的真实明细查询和 `OPER-02` 的 CSV 导出都已经绑定到同一后端语义，并具备 SQL 与真实 CSV 行为验证路径。
</success_criteria>

<output>
After completion, create `.planning/phases/05-drilldown-and-release-hardening/05-drilldown-and-release-hardening-02-SUMMARY.md`
</output>
