---
phase: 05-drilldown-and-release-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - frontend/src/app/manager-drilldown-page.tsx
  - frontend/src/components/export-cases-button.tsx
  - frontend/src/components/problem-case-table.tsx
  - frontend/src/lib/manager-drilldown-api.ts
  - src/api/http_dashboard_server.py
  - src/api/http_dashboard_export.py
  - src/api/security/viewer_context.py
  - src/api/security/role_access_smoke.py
  - src/domain/analytics/contracts.py
  - src/domain/analytics/query_service.py
  - sql/tests/oper_02_case_export.sql
  - sql/tests/run-phase-01.sh
autonomous: true
requirements:
  - OPER-02
must_haves:
  truths:
    - "管理者能从当前下钻条件直接导出病例列表，而不是手工从页面复制。"
    - "导出文件与明细页使用同一组过滤条件和同一条后端查询语义。"
    - "导出响应由服务端生成下载内容，前端只负责发起下载，不在浏览器重算数据。"
    - "导出链路保留病例人工复核需要的核心字段，并与当前 drilldown 目标一致。"
  artifacts:
    - path: "src/api/http_dashboard_export.py"
      provides: "CSV export endpoint for filtered problem-case rows"
      contains: "text/csv"
    - path: "frontend/src/components/export-cases-button.tsx"
      provides: "Explicit manager-side export trigger tied to current drilldown filters"
      contains: "导出"
    - path: "sql/tests/oper_02_case_export.sql"
      provides: "SQL assertion proving export row set matches drilldown semantics"
      contains: "copy|select|csv"
  key_links:
    - from: "frontend/src/components/export-cases-button.tsx"
      to: "src/api/http_dashboard_export.py"
      via: "frontend serializes current drilldown filters into an export request"
      pattern: "/api/analytics/drilldown/export"
    - from: "src/api/http_dashboard_export.py"
      to: "src/domain/analytics/query_service.py"
      via: "export route reuses the canonical problem-case row query helper"
      pattern: "get_problem_case_rows|get_manager_drilldown"
    - from: "src/api/security/viewer_context.py"
      to: "src/api/http_dashboard_export.py"
      via: "request-level role gate blocks non-manager exports"
      pattern: "manager"
---

<objective>
交付问题病例导出能力，让管理者能把当前筛选下的病例明细下载为复核文件。

Purpose: 完成 `OPER-02`，确保管理者看到什么就能导出什么，并让导出复用 05-01 的明细查询语义而不是另起一套逻辑。
Output: CSV export endpoint、frontend export trigger、viewer context parser、role-aware export smoke、SQL export semantics assertions.
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-drilldown-and-release-hardening/05-RESEARCH.md
@.planning/phases/05-drilldown-and-release-hardening/05-VALIDATION.md
@.planning/phases/05-drilldown-and-release-hardening/05-01-PLAN.md
@frontend/src/lib/analytics-filters.ts
@frontend/src/app/manager-drilldown-page.tsx
@src/api/http_dashboard_server.py
@src/domain/analytics/query_service.py

<interfaces>
Plan 05-02 should reuse the 05-01 drilldown request shape and add an export response boundary like:
```http
GET /api/analytics/drilldown/export?...filters...
Content-Type: text/csv; charset=utf-8
Content-Disposition: attachment; filename="problem-cases-YYYYMMDD.csv"
```

Role context should stay narrow:
```py
@dataclass(slots=True)
class ViewerContext:
    role: Literal["manager", "doctor"]
    viewer_id: str | None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CSV export endpoint and manager-side download trigger</name>
  <files>frontend/src/app/manager-drilldown-page.tsx, frontend/src/components/export-cases-button.tsx, frontend/src/components/problem-case-table.tsx, frontend/src/lib/manager-drilldown-api.ts, src/api/http_dashboard_server.py, src/api/http_dashboard_export.py, src/domain/analytics/contracts.py, src/domain/analytics/query_service.py</files>
  <action>Build the export path directly on top of the canonical drilldown query from 05-01. Add `src/api/http_dashboard_export.py` and register it from `src/api/http_dashboard_server.py` as a manager analytics GET route that returns CSV with a stable filename and header row. Extend `src/domain/analytics/query_service.py` only as needed so the export path and the on-screen detail path share the same filtered case-row helper. Add `frontend/src/components/export-cases-button.tsx` and wire it into `frontend/src/app/manager-drilldown-page.tsx` so the current drilldown state launches a server-generated download instead of reassembling CSV in the browser. Keep the exported columns grounded in manual review needs: identifiers, patient/doctor/department/disease context, evaluation flags, and trigger time. Reuse explicit filter serialization from the manager drilldown API layer so screen and file stay aligned.</action>
  <verify>
    <automated>python3 -m py_compile src/api/http_dashboard_export.py src/domain/analytics/query_service.py && python3 src/api/security/role_access_smoke.py</automated>
  </verify>
  <done>管理者已经可以从当前 drilldown 条件直接发起服务端 CSV 导出，且导出与页面筛选一致。</done>
</task>

<task type="auto">
  <name>Task 2: Lock export semantics with SQL assertions and role-aware smoke coverage</name>
  <files>src/api/security/viewer_context.py, src/api/security/role_access_smoke.py, src/api/http_dashboard_export.py, sql/tests/oper_02_case_export.sql, sql/tests/run-phase-01.sh</files>
  <action>Finish `OPER-02` hardening by adding request-level context parsing and deterministic verification. Add `src/api/security/viewer_context.py` to parse the host-provided viewer role and identity assumptions used by Phase 5, but keep it intentionally narrow and consistent with the repo’s current request model rather than inventing a full IAM stack. Add or extend `src/api/security/role_access_smoke.py` so it proves manager export succeeds while non-manager access is rejected. Add `sql/tests/oper_02_case_export.sql` and register it in `sql/tests/run-phase-01.sh` to assert the exported row set matches the same filtered case set used by drilldown, including Phase 4 persisted evaluation rows. Keep export synchronous and lightweight; do not plan async jobs or notification infrastructure.</action>
  <verify>
    <automated>python3 -m py_compile src/api/http_dashboard_export.py src/api/security/viewer_context.py src/api/security/role_access_smoke.py && python3 src/api/security/role_access_smoke.py && export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh oper_02_case_export</automated>
  </verify>
  <done>`OPER-02` 已有自动验证的导出语义和角色边界，不会因为页面或 SQL 漂移造成导出结果失真。</done>
</task>

</tasks>

<verification>
执行 `python3 -m py_compile src/api/http_dashboard_export.py src/api/security/viewer_context.py src/api/security/role_access_smoke.py && python3 src/api/security/role_access_smoke.py`，再执行 `export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh oper_02_case_export`。
</verification>

<success_criteria>
`OPER-02` 的导出链路、服务端 CSV 生成、角色门禁和 SQL 语义一致性都已具备自动验证。
</success_criteria>

<output>
After completion, create `.planning/phases/05-drilldown-and-release-hardening/05-drilldown-and-release-hardening-02-SUMMARY.md`
</output>
