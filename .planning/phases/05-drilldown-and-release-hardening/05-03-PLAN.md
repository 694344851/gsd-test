---
phase: 05-drilldown-and-release-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - frontend/src/app/dashboard-overview-page.tsx
  - frontend/src/app/doctor-evaluation-page.tsx
  - frontend/src/app/__tests__/manager-drilldown-page.test.tsx
  - frontend/src/app/__tests__/doctor-evaluation-page.test.tsx
  - frontend/src/lib/viewer-context-contracts.ts
  - frontend/src/lib/doctor-evaluation-embed.tsx
  - src/api/http_dashboard_server.py
  - src/api/http_dashboard_drilldown.py
  - src/api/http_dashboard_export.py
  - src/api/security/viewer_context.py
  - src/api/security/role_access_smoke.py
  - src/domain/analytics/query_service.py
  - sql/tests/run-phase-01.sh
autonomous: true
requirements:
  - OPER-03
  - OPER-01
  - OPER-02
must_haves:
  truths:
    - "医生端和管理端入口边界明确，医生不会获得 manager-only 下钻/导出访问。"
    - "manager-only 路由在服务端拒绝越权请求，不能只靠前端隐藏按钮。"
    - "Phase 5 上线前回归同时覆盖首页下钻、导出、医生端诊鉴和 Phase 4 持久化数据复用。"
    - "发布准备明确记录 host 需要提供的 viewer_role 假设，而不扩展成完整 IAM 平台。"
  artifacts:
    - path: "src/api/security/viewer_context.py"
      provides: "Single request-level viewer role parser for manager/doctor boundaries"
      contains: "ViewerContext"
    - path: "frontend/src/lib/viewer-context-contracts.ts"
      provides: "Frontend bootstrap contract for host-provided role context"
      contains: "viewer_role"
    - path: "src/api/security/role_access_smoke.py"
      provides: "Role-boundary smoke coverage across drilldown/export routes"
      contains: "403"
  key_links:
    - from: "frontend/src/lib/viewer-context-contracts.ts"
      to: "frontend/src/app/dashboard-overview-page.tsx"
      via: "manager shell renders drilldown/export affordances only when host bootstrap marks a manager viewer"
      pattern: "manager|doctor"
    - from: "frontend/src/lib/doctor-evaluation-embed.tsx"
      to: "frontend/src/app/doctor-evaluation-page.tsx"
      via: "doctor embed remains isolated from dashboard drilldown routing"
      pattern: "mountDoctorEvaluationEmbed"
    - from: "src/api/security/viewer_context.py"
      to: "src/api/http_dashboard_drilldown.py|src/api/http_dashboard_export.py"
      via: "request-level gating rejects non-manager access with 403"
      pattern: "403|forbidden"
---

<objective>
完成角色权限边界、跨 Phase 回归验证和上线前收口，确保下钻、导出和医生端嵌入链路能一起安全发布。

Purpose: 完成 `OPER-03` 并为 Phase 5 收尾，把角色边界、回归验证和 host 接入假设明确下来，避免上线时出现 doctor/manager 串场或指标语义断裂。
Output: frontend viewer context contracts、server-side role gates、role smoke coverage、cross-phase regression checks、release-readiness notes in plan execution summary.
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-drilldown-and-release-hardening/05-RESEARCH.md
@.planning/phases/05-drilldown-and-release-hardening/05-VALIDATION.md
@.planning/phases/05-drilldown-and-release-hardening/05-01-PLAN.md
@.planning/phases/05-drilldown-and-release-hardening/05-02-PLAN.md
@.planning/phases/04-realtime-evaluation-workflow/04-realtime-evaluation-workflow-01-SUMMARY.md
@.planning/phases/04-realtime-evaluation-workflow/04-realtime-evaluation-workflow-03-SUMMARY.md
@frontend/src/app/doctor-evaluation-page.tsx
@frontend/src/lib/doctor-evaluation-embed.tsx
@src/api/http_dashboard_server.py

<interfaces>
Phase 5 release hardening should keep role context explicit and narrow:
```ts
export interface ViewerBootstrapContext {
  viewer_role: 'manager' | 'doctor';
  viewer_id?: string;
  viewer_name?: string;
}
```

Manager-only routes should reject non-manager requests with:
```json
{ "error": "forbidden", "message": "manager access required" }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce explicit role boundaries across manager and doctor surfaces</name>
  <files>frontend/src/app/dashboard-overview-page.tsx, frontend/src/app/doctor-evaluation-page.tsx, frontend/src/lib/viewer-context-contracts.ts, frontend/src/lib/doctor-evaluation-embed.tsx, src/api/http_dashboard_server.py, src/api/http_dashboard_drilldown.py, src/api/http_dashboard_export.py, src/api/security/viewer_context.py, src/api/security/role_access_smoke.py</files>
  <action>Implement the minimum credible role boundary per `OPER-03` and the research constraints. Add `frontend/src/lib/viewer-context-contracts.ts` to document the host bootstrap shape for `viewer_role` and related identity hints, then update manager-side bootstrap/rendering so drilldown/export affordances only appear when the host marks a manager viewer. Keep the doctor-side `mountDoctorEvaluationEmbed(...)` flow isolated and free of manager routing assumptions. On the backend, centralize request-level role parsing in `src/api/security/viewer_context.py` and enforce manager-only access inside `src/api/http_dashboard_drilldown.py` and `src/api/http_dashboard_export.py`; UI hiding alone is not sufficient. Extend `src/api/security/role_access_smoke.py` so it exercises allowed manager access and forbidden doctor access with explicit 403 assertions. Do not introduce SSO, RBAC administration, or a broader IAM platform.</action>
  <verify>
    <automated>python3 -m py_compile src/api/http_dashboard_server.py src/api/http_dashboard_drilldown.py src/api/http_dashboard_export.py src/api/security/viewer_context.py src/api/security/role_access_smoke.py && python3 src/api/security/role_access_smoke.py</automated>
  </verify>
  <done>医生端与管理端的入口、路由和请求权限边界已经被显式锁定，满足 `OPER-03` 的核心访问控制要求。</done>
</task>

<task type="auto">
  <name>Task 2: Run cross-phase regression coverage and codify release readiness</name>
  <files>frontend/src/app/__tests__/manager-drilldown-page.test.tsx, frontend/src/app/__tests__/doctor-evaluation-page.test.tsx, src/domain/analytics/query_service.py, sql/tests/run-phase-01.sh</files>
  <action>Close Phase 5 with automated regression checks and release-prep notes grounded in current repo reality. Extend or stabilize the existing manager and doctor page tests so the manager drilldown shell and the doctor evaluation shell can both be verified in one pass without crossing responsibilities. Ensure `sql/tests/run-phase-01.sh` can execute the Phase 5 drilldown/export assertions together with `eval_01_persistence_chain` so Phase 4 persisted evaluation data remains visible to manager detail/export flows. If `src/domain/analytics/query_service.py` needs minor adjustments for regression stability, keep them limited to preserving Phase 1/4 semantics rather than adding new business logic. The execution summary for this plan must explicitly record remaining host-side assumptions: a trusted bootstrap `viewer_role` source and separate manager/doctor mount contexts.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/app/__tests__/manager-drilldown-page.test.tsx src/app/__tests__/doctor-evaluation-page.test.tsx && export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh oper_01_problem_drilldown oper_02_case_export eval_01_persistence_chain</automated>
  </verify>
  <done>Phase 5 的关键新旧链路已一起通过自动回归，执行总结可以直接作为上线前收口依据。</done>
</task>

</tasks>

<verification>
执行 `python3 -m py_compile src/api/http_dashboard_server.py src/api/http_dashboard_drilldown.py src/api/http_dashboard_export.py src/api/security/viewer_context.py src/api/security/role_access_smoke.py && python3 src/api/security/role_access_smoke.py`，再执行 `cd frontend && npm test -- --run src/app/__tests__/manager-drilldown-page.test.tsx src/app/__tests__/doctor-evaluation-page.test.tsx`，最后执行 `export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh oper_01_problem_drilldown oper_02_case_export eval_01_persistence_chain`。
</verification>

<success_criteria>
`OPER-03` 和 Phase 5 发布收口要求已经体现在服务端门禁、前端角色边界、跨 Phase 自动回归和执行总结的 host 假设记录中。
</success_criteria>

<output>
After completion, create `.planning/phases/05-drilldown-and-release-hardening/05-drilldown-and-release-hardening-03-SUMMARY.md`
</output>
