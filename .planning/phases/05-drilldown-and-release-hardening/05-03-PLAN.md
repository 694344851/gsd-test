---
phase: 05-drilldown-and-release-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - frontend/src/app/dashboard-overview-page.tsx
  - frontend/src/app/doctor-evaluation-page.tsx
  - frontend/src/app/__tests__/manager-drilldown-page.test.tsx
  - frontend/src/app/__tests__/doctor-evaluation-page.test.tsx
  - frontend/src/lib/viewer-context-contracts.ts
  - frontend/src/lib/doctor-evaluation-embed.tsx
  - src/api/http_dashboard_server.py
  - src/api/http_dashboard_drilldown.py
  - src/api/http_dashboard_export.py
  - src/api/security/viewer_context.py
  - src/api/security/role_access_smoke.py
  - .planning/phases/05-drilldown-and-release-hardening/05-HUMAN-UAT.md
  - .planning/phases/05-drilldown-and-release-hardening/05-VERIFICATION.md
autonomous: true
requirements:
  - OPER-03
  - OPER-01
  - OPER-02
must_haves:
  truths:
    - "角色边界必须由服务端统一校验，前端隐藏按钮只能作为补充体验，不算授权。"
    - "医生端嵌入式诊鉴和管理端下钻导出必须保持两个独立 surface，不能因为 Phase 5 引入串场。"
    - "发布收口必须回归验证首页→下钻→导出链路以及医生端 realtime 入口，不接受只验证新增接口。"
    - "Phase 5 结束时应留下明确的自动化回归结果和人工验收清单。"
  artifacts:
    - path: "src/api/security/viewer_context.py"
      provides: "Shared viewer-role parsing and validation"
      contains: "viewer_role"
    - path: ".planning/phases/05-drilldown-and-release-hardening/05-HUMAN-UAT.md"
      provides: "Human verification checklist for release hardening"
      contains: "Test 1"
    - path: ".planning/phases/05-drilldown-and-release-hardening/05-VERIFICATION.md"
      provides: "Phase verification report"
      contains: "verification"
  key_links:
    - from: "frontend/src/lib/viewer-context-contracts.ts"
      to: "src/api/security/viewer_context.py"
      via: "frontend and backend share the same viewer-role vocabulary"
      pattern: "manager|doctor"
    - from: "frontend/src/lib/doctor-evaluation-embed.tsx"
      to: "src/api/security/viewer_context.py"
      via: "doctor embed assumptions stay separate from manager-only dashboard routes"
      pattern: "doctor"
    - from: "src/api/http_dashboard_drilldown.py"
      to: "src/api/http_dashboard_export.py"
      via: "both manager routes enforce the same role gate before serving data"
      pattern: "forbidden"
---

<objective>
完成角色权限、验收和发布准备，确保医生端与管理端边界清晰，且跨 Phase 关键链路在上线前有明确验证闭环。

Purpose: 完成 OPER-03，并把 Phase 5 从功能交付推进到可发布状态，避免出现越权访问或跨端串场。
Output: shared viewer-role contract、route-level access checks、cross-phase regression tests、verification report、human UAT checklist。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-drilldown-and-release-hardening/05-RESEARCH.md
@.planning/phases/05-drilldown-and-release-hardening/05-VALIDATION.md
@.planning/phases/05-drilldown-and-release-hardening/05-01-PLAN.md
@.planning/phases/05-drilldown-and-release-hardening/05-02-PLAN.md
@.planning/phases/04-realtime-evaluation-workflow/04-VERIFICATION.md
@.planning/phases/04-realtime-evaluation-workflow/04-HUMAN-UAT.md
@frontend/src/lib/doctor-evaluation-embed.tsx
@src/api/http_dashboard_server.py
@门诊诊鉴首页-(全栈).md

<interfaces>
Plan 05-03 must preserve a shared viewer context contract shaped like:
```ts
export type ViewerRole = 'manager' | 'doctor';

export interface ViewerContext {
  viewer_role: ViewerRole;
  viewer_id?: string;
  viewer_name?: string;
}
```

And local/dev verification must have a concrete host contract shaped like:
```http
X-Viewer-Role: manager | doctor
X-Viewer-Id: test-viewer-001
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce doctor/manager role boundaries across frontend and backend surfaces</name>
  <read_first>
    .planning/phases/05-drilldown-and-release-hardening/05-RESEARCH.md
    .planning/phases/05-drilldown-and-release-hardening/05-01-PLAN.md
    .planning/phases/05-drilldown-and-release-hardening/05-02-PLAN.md
    frontend/src/lib/doctor-evaluation-embed.tsx
    src/api/security/viewer_context.py
  </read_first>
  <files>frontend/src/app/dashboard-overview-page.tsx, frontend/src/app/doctor-evaluation-page.tsx, frontend/src/app/__tests__/manager-drilldown-page.test.tsx, frontend/src/app/__tests__/doctor-evaluation-page.test.tsx, frontend/src/lib/viewer-context-contracts.ts, frontend/src/lib/doctor-evaluation-embed.tsx, src/api/http_dashboard_server.py, src/api/http_dashboard_drilldown.py, src/api/http_dashboard_export.py, src/api/security/viewer_context.py, src/api/security/role_access_smoke.py</files>
  <acceptance_criteria>
    - `frontend/src/lib/viewer-context-contracts.ts` defines the shared viewer-role vocabulary used by doctor and manager surfaces.
    - `src/api/security/viewer_context.py` supports a concrete local/dev contract such as `X-Viewer-Role` and `X-Viewer-Id` so smoke tests do not depend on an external host.
    - Manager-only UI entrypoints are hidden or disabled for doctor viewers, but backend manager routes still reject non-manager requests with explicit 403 behavior.
    - Doctor embed assumptions remain intact and do not depend on manager-only drilldown/export state.
    - `src/api/security/role_access_smoke.py` proves drilldown and export routes reject doctor access and accept manager access using the documented local/dev viewer headers.
    - Frontend tests cover at least one doctor-view and one manager-view boundary behavior.
  </acceptance_criteria>
  <action>Implement the release-grade role boundary for doctor and manager surfaces. Add `frontend/src/lib/viewer-context-contracts.ts` to define a shared viewer context vocabulary and use it to make manager-only dashboard entrypoints conditional without polluting the doctor embed surface. Update `frontend/src/lib/doctor-evaluation-embed.tsx`, `frontend/src/app/doctor-evaluation-page.tsx`, and `frontend/src/app/dashboard-overview-page.tsx` as needed so doctor and manager flows remain isolated but explicit. On the backend, centralize viewer-role parsing and validation in `src/api/security/viewer_context.py`, explicitly document and support a local/dev contract such as `X-Viewer-Role` and `X-Viewer-Id`, then apply it consistently to the drilldown and export handlers through `src/api/http_dashboard_server.py`, `src/api/http_dashboard_drilldown.py`, and `src/api/http_dashboard_export.py`. Add or extend frontend and smoke tests so both UI behavior and route-level 403 semantics are exercised against that documented contract.</action>
  <verify>
    <automated>python3 -m py_compile src/api/http_dashboard_server.py src/api/http_dashboard_drilldown.py src/api/http_dashboard_export.py src/api/security/viewer_context.py src/api/security/role_access_smoke.py && python3 src/api/security/role_access_smoke.py && cd frontend && npm test -- --run src/app/__tests__/manager-drilldown-page.test.tsx src/app/__tests__/doctor-evaluation-page.test.tsx</automated>
  </verify>
  <done>医生端与管理端已经形成可验证的权限边界，不再依赖 UI 约定式隔离。</done>
</task>

<task type="auto">
  <name>Task 2: Produce cross-phase verification artifacts and release-readiness handoff</name>
  <read_first>
    .planning/phases/05-drilldown-and-release-hardening/05-RESEARCH.md
    .planning/phases/05-drilldown-and-release-hardening/05-VALIDATION.md
    .planning/phases/04-realtime-evaluation-workflow/04-VERIFICATION.md
    .planning/phases/04-realtime-evaluation-workflow/04-HUMAN-UAT.md
    sql/tests/oper_01_problem_drilldown.sql
    sql/tests/oper_02_case_export.sql
  </read_first>
  <files>.planning/phases/05-drilldown-and-release-hardening/05-HUMAN-UAT.md, .planning/phases/05-drilldown-and-release-hardening/05-VERIFICATION.md</files>
  <acceptance_criteria>
    - Cross-phase regression verifies homepage filters, drilldown rows, export rows, and persisted realtime evaluation data remain semantically aligned.
    - `.planning/phases/05-drilldown-and-release-hardening/05-VERIFICATION.md` records the automated verification outcomes and any residual risks.
    - `.planning/phases/05-drilldown-and-release-hardening/05-HUMAN-UAT.md` captures the manual checks required before release.
    - The release-readiness artifact set references both manager workflow and doctor embed regression coverage.
  </acceptance_criteria>
  <action>Close Phase 5 with explicit release-hardening artifacts. Reuse the automated coverage produced by 05-01 and 05-02, plus the Phase 4 realtime verification artifacts, to summarize whether homepage filters, drilldown rows, export rows, and persisted realtime evaluation reuse are all release-ready. Create `.planning/phases/05-drilldown-and-release-hardening/05-VERIFICATION.md` summarizing the automated checks, their outcomes, and any non-blocking residual risks, and create `.planning/phases/05-drilldown-and-release-hardening/05-HUMAN-UAT.md` with the minimal manual release checklist for real manager review flow and real host role-context validation.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/app/__tests__/manager-drilldown-page.test.tsx src/app/__tests__/doctor-evaluation-page.test.tsx && python3 src/api/security/role_access_smoke.py && export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh oper_01_problem_drilldown oper_02_case_export eval_01_persistence_chain && test -f .planning/phases/05-drilldown-and-release-hardening/05-VERIFICATION.md && test -f .planning/phases/05-drilldown-and-release-hardening/05-HUMAN-UAT.md && rg -n \"verification|风险|residual\" .planning/phases/05-drilldown-and-release-hardening/05-VERIFICATION.md && rg -n \"Test 1|Test 2|pass\" .planning/phases/05-drilldown-and-release-hardening/05-HUMAN-UAT.md</automated>
  </verify>
  <done>Phase 5 已经留下自动化回归结果与人工验收清单，可直接进入 verify-work 或上线前人工确认。</done>
</task>

</tasks>

<verification>
执行 `python3 -m py_compile src/api/http_dashboard_server.py src/api/http_dashboard_drilldown.py src/api/http_dashboard_export.py src/api/security/viewer_context.py src/api/security/role_access_smoke.py`，再执行 `python3 src/api/security/role_access_smoke.py`，再执行 `cd frontend && npm test -- --run src/app/__tests__/manager-drilldown-page.test.tsx src/app/__tests__/doctor-evaluation-page.test.tsx`，再执行 `export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh oper_01_problem_drilldown oper_02_case_export eval_01_persistence_chain`，最后执行 `test -f .planning/phases/05-drilldown-and-release-hardening/05-VERIFICATION.md && test -f .planning/phases/05-drilldown-and-release-hardening/05-HUMAN-UAT.md`。
</verification>

<success_criteria>
`OPER-03` 对应的角色边界和 Phase 5 上线前收口工作已经被拆成可执行、可验证、可交付的计划。
</success_criteria>

<output>
After completion, create `.planning/phases/05-drilldown-and-release-hardening/05-drilldown-and-release-hardening-03-SUMMARY.md`
</output>
