---
phase: 05-drilldown-and-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/dashboard-overview-page.tsx
  - frontend/src/app/manager-drilldown-page.tsx
  - frontend/src/app/__tests__/manager-drilldown-page.test.tsx
  - frontend/src/components/distribution-section.tsx
  - frontend/src/components/disease-insights-section.tsx
  - frontend/src/components/__tests__/distribution-section.test.tsx
  - frontend/src/components/__tests__/disease-insights-section.test.tsx
  - frontend/src/components/problem-case-table.tsx
  - frontend/src/components/drilldown-filter-summary.tsx
  - frontend/src/lib/manager-drilldown-api.ts
  - frontend/src/lib/manager-drilldown-contracts.ts
autonomous: true
requirements:
  - OPER-01
must_haves:
  truths:
    - "首页下钻必须形成 URL 可寻址状态，刷新、回退和复制链接后不能丢失筛选意图。"
    - "明细页和首页必须复用同一套时间范围与筛选语义，前端不能自行重新定义指标口径。"
    - "05-01 只交付前端 drilldown 壳和导航契约，真实服务端明细查询与导出语义在 05-02 落地。"
    - "首页下钻入口必须显式覆盖科室、医生、病种三种粒度，不能缺 doctor。"
  artifacts:
    - path: "frontend/src/app/manager-drilldown-page.tsx"
      provides: "Manager drilldown shell with URL-driven filter state"
      contains: "ManagerDrilldownPage"
    - path: "frontend/src/lib/manager-drilldown-api.ts"
      provides: "Typed manager drilldown client seam for later backend wiring"
      contains: "loadManagerDrilldown"
    - path: "frontend/src/components/distribution-section.tsx"
      provides: "Homepage drilldown entrypoint for department and doctor"
      contains: "doctor"
  key_links:
    - from: "frontend/src/components/distribution-section.tsx"
      to: "frontend/src/app/manager-drilldown-page.tsx"
      via: "distribution interactions open the manager drilldown with department or doctor scope encoded in URL state"
      pattern: "department|doctor"
    - from: "frontend/src/components/disease-insights-section.tsx"
      to: "frontend/src/app/manager-drilldown-page.tsx"
      via: "disease insight interactions open the same drilldown shell for disease-level detail"
      pattern: "disease"
    - from: "frontend/src/app/manager-drilldown-page.tsx"
      to: "frontend/src/lib/manager-drilldown-api.ts"
      via: "detail shell loads rows through an injectable typed client seam that 05-02 can bind to real backend data"
      pattern: "loadManagerDrilldown"
---

<objective>
交付管理端问题病例下钻的前端主链路，包括首页入口、URL 驱动的明细页和可验证的病例表格壳。

Purpose: 先完成 OPER-01 的前端边界，让管理者可以从现有首页信号进入问题病例明细壳，并把 URL/state/typed contracts 固定下来，再由 05-02 接真实服务端数据。
Output: manager drilldown shell、typed drilldown contracts、homepage click-through wiring、detail-table shell、frontend verification。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-drilldown-and-release-hardening/05-RESEARCH.md
@.planning/phases/05-drilldown-and-release-hardening/05-VALIDATION.md
@frontend/src/app/dashboard-overview-page.tsx
@frontend/src/components/distribution-section.tsx
@frontend/src/components/disease-insights-section.tsx
@门诊诊鉴首页-(全栈).md

<interfaces>
Plan 05-01 must establish a drilldown contract shaped like:
```ts
export interface DrilldownIntent {
  dimension: 'department' | 'doctor' | 'disease';
  dimension_value: string;
  dimension_label: string;
  source_module: 'distribution' | 'disease_insights' | 'overview';
  filters: DashboardFilters;
}

export interface ProblemCaseRow {
  encounter_id: string;
  patient_name: string;
  department_name: string;
  doctor_name: string;
  primary_diagnosis_name: string;
  evaluation_status: 'success' | 'timeout' | 'failed';
  diagnosis_basis_incomplete: boolean;
  missing_diagnosis: boolean;
  triggered_at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add URL-driven manager drilldown shell and homepage entry wiring</name>
  <read_first>
    .planning/phases/05-drilldown-and-release-hardening/05-RESEARCH.md
    frontend/src/app/dashboard-overview-page.tsx
    frontend/src/components/distribution-section.tsx
    frontend/src/components/disease-insights-section.tsx
    frontend/src/lib/analytics-filters.ts
  </read_first>
  <files>frontend/src/app/dashboard-overview-page.tsx, frontend/src/app/manager-drilldown-page.tsx, frontend/src/app/__tests__/manager-drilldown-page.test.tsx, frontend/src/components/distribution-section.tsx, frontend/src/components/disease-insights-section.tsx, frontend/src/components/problem-case-table.tsx, frontend/src/components/drilldown-filter-summary.tsx, frontend/src/components/__tests__/distribution-section.test.tsx, frontend/src/components/__tests__/disease-insights-section.test.tsx, frontend/src/lib/manager-drilldown-contracts.ts, frontend/src/lib/manager-drilldown-api.ts</files>
  <acceptance_criteria>
    - `frontend/src/app/manager-drilldown-page.tsx` renders a manager-facing detail shell driven by URL/search state rather than hidden local state.
    - `frontend/src/components/distribution-section.tsx` exposes drilldown interactions that carry current filters and selected department or doctor context into the manager drilldown flow.
    - `frontend/src/components/disease-insights-section.tsx` exposes a drilldown interaction for disease-level detail using the same typed intent model.
    - `frontend/src/components/problem-case-table.tsx` renders typed case rows with stable columns suitable for later export reuse.
    - `frontend/src/app/__tests__/manager-drilldown-page.test.tsx` verifies URL state parsing and detail-shell rendering.
    - Existing section tests prove the click-through interactions emit the correct department, doctor, and disease drilldown intents instead of only hover behavior.
  </acceptance_criteria>
  <action>Implement the manager-side drilldown UI path. Add `frontend/src/lib/manager-drilldown-contracts.ts` to define typed drilldown intent, request, response, and case-row contracts that explicitly reuse the existing dashboard filter model. Build `frontend/src/app/manager-drilldown-page.tsx` as a URL-driven shell that can derive drilldown dimension, selected value, and filters from route/search state, then render summary context plus a problem-case table shell. Extend `frontend/src/components/distribution-section.tsx` so it can emit both department-level and doctor-level drilldown intents from the homepage context, and extend `frontend/src/components/disease-insights-section.tsx` for disease-level entry. Add `frontend/src/components/problem-case-table.tsx` and `frontend/src/components/drilldown-filter-summary.tsx` to keep case-level detail readable and deterministic. Update or add focused Vitest coverage proving the drilldown state survives URL parsing and that homepage modules emit the correct typed navigation intent for all three required dimensions.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/__tests__/distribution-section.test.tsx src/components/__tests__/disease-insights-section.test.tsx src/app/__tests__/manager-drilldown-page.test.tsx</automated>
  </verify>
  <done>管理端已经具备从首页进入 department/doctor/disease 三种问题病例明细的 URL 驱动入口，OPER-01 的前端骨架成立。</done>
</task>

<task type="auto">
  <name>Task 2: Add typed drilldown client seam and loading/error states for later backend binding</name>
  <read_first>
    .planning/phases/05-drilldown-and-release-hardening/05-RESEARCH.md
    frontend/src/app/manager-drilldown-page.tsx
    frontend/src/components/problem-case-table.tsx
    frontend/src/lib/manager-drilldown-contracts.ts
  </read_first>
  <files>frontend/src/app/manager-drilldown-page.tsx, frontend/src/app/__tests__/manager-drilldown-page.test.tsx, frontend/src/components/problem-case-table.tsx, frontend/src/components/drilldown-filter-summary.tsx, frontend/src/lib/manager-drilldown-api.ts, frontend/src/lib/manager-drilldown-contracts.ts</files>
  <acceptance_criteria>
    - `frontend/src/lib/manager-drilldown-api.ts` exposes a typed client seam such as `loadManagerDrilldown(...)`.
    - `frontend/src/app/manager-drilldown-page.tsx` handles loading, empty, and error states through the typed client seam rather than embedding fake static rows inline.
    - The page shell remains backend-agnostic so 05-02 can bind the real drilldown endpoint without rewriting the UI state model.
    - `frontend/src/app/__tests__/manager-drilldown-page.test.tsx` verifies loading, empty, and error behavior through the injected client seam.
  </acceptance_criteria>
  <action>Implement the typed data seam that 05-02 will bind to the real backend. Add `frontend/src/lib/manager-drilldown-api.ts` with a typed `loadManagerDrilldown(...)` boundary returning summary and case rows for any department, doctor, or disease drilldown request. Update `frontend/src/app/manager-drilldown-page.tsx` so it loads through that seam and renders deterministic loading, empty, and error states instead of mixing routing and data assumptions. Keep `frontend/src/components/problem-case-table.tsx` and `frontend/src/components/drilldown-filter-summary.tsx` presentation-only so the real server query path can drop in later without churn. Extend frontend tests to prove the page shell behaves correctly through the injected seam.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/app/__tests__/manager-drilldown-page.test.tsx src/components/__tests__/distribution-section.test.tsx src/components/__tests__/disease-insights-section.test.tsx</automated>
  </verify>
  <done>manager drilldown 页面已经和真实后端实现解耦，05-02 可以在不重写 UI 状态机的前提下接入真实查询链路。</done>
</task>

</tasks>

<verification>
执行 `cd frontend && npm test -- --run src/components/__tests__/distribution-section.test.tsx src/components/__tests__/disease-insights-section.test.tsx src/app/__tests__/manager-drilldown-page.test.tsx`。
</verification>

<success_criteria>
`OPER-01` 所需的首页下钻入口、URL 驱动明细页和 typed client seam 都已具备明确执行边界，且 department/doctor/disease 三种入口都有覆盖。
</success_criteria>

<output>
After completion, create `.planning/phases/05-drilldown-and-release-hardening/05-drilldown-and-release-hardening-01-SUMMARY.md`
</output>
