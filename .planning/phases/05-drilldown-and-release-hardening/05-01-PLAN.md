---
phase: 05-drilldown-and-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/dashboard-overview-page.tsx
  - frontend/src/app/manager-drilldown-page.tsx
  - frontend/src/app/__tests__/manager-drilldown-page.test.tsx
  - frontend/src/components/distribution-section.tsx
  - frontend/src/components/disease-insights-section.tsx
  - frontend/src/components/__tests__/distribution-section.test.tsx
  - frontend/src/components/__tests__/disease-insights-section.test.tsx
  - frontend/src/components/problem-case-table.tsx
  - frontend/src/components/manager-drilldown-header.tsx
  - frontend/src/components/drilldown-filter-summary.tsx
  - frontend/src/lib/manager-drilldown-contracts.ts
  - frontend/src/lib/manager-drilldown-api.ts
  - src/api/http_dashboard_server.py
  - src/api/http_dashboard_drilldown.py
  - src/api/analytics/drilldown_smoke.py
  - src/domain/analytics/contracts.py
  - src/domain/analytics/query_service.py
  - sql/tests/oper_01_problem_drilldown.sql
  - sql/tests/run-phase-01.sh
autonomous: true
requirements:
  - OPER-01
must_haves:
  truths:
    - "管理者可以从首页科室分布或高发问题病种入口进入问题病例明细，而不是停留在聚合图表。"
    - "下钻状态在 URL 中可见并能在刷新后恢复，不能只依赖页面局部 state。"
    - "下钻明细继续沿用首页时间/筛选语义，并复用 Phase 4 已持久化评估结果。"
    - "明细页至少能按科室、医生、病种三种维度表达当前下钻目标。"
  artifacts:
    - path: "frontend/src/app/manager-drilldown-page.tsx"
      provides: "Manager drilldown detail shell with URL-addressable state"
      contains: "window.location.search|URLSearchParams"
    - path: "frontend/src/lib/manager-drilldown-contracts.ts"
      provides: "Typed drilldown intent/request/response contracts reusing dashboard filters"
      contains: "DrilldownIntent"
    - path: "src/api/http_dashboard_drilldown.py"
      provides: "GET detail endpoint for problem-case drilldown"
      contains: "handle_drilldown"
    - path: "sql/tests/oper_01_problem_drilldown.sql"
      provides: "SQL assertion for drilldown row semantics over persisted evaluation data"
      contains: "analytics"
  key_links:
    - from: "frontend/src/components/distribution-section.tsx"
      to: "frontend/src/app/manager-drilldown-page.tsx"
      via: "homepage click-through emits a typed drilldown intent encoded into the URL"
      pattern: "DrilldownIntent|URLSearchParams"
    - from: "frontend/src/components/disease-insights-section.tsx"
      to: "frontend/src/app/manager-drilldown-page.tsx"
      via: "disease insight selection opens the same detail shell with explicit dimension filters"
      pattern: "disease|drilldown"
    - from: "src/api/http_dashboard_drilldown.py"
      to: "src/domain/analytics/query_service.py"
      via: "detail handler delegates row resolution to one backend query helper"
      pattern: "get_problem_case_rows|get_manager_drilldown"
---

<objective>
交付管理端问题病例下钻页和首页条件联动，让首页聚合信号能进入可复核的病例明细视图。

Purpose: 完成 `OPER-01`，把首页的科室/病种聚合洞察接到稳定的病例级明细查询，同时把 drilldown 状态落到 URL，避免刷新后丢失上下文。
Output: manager drilldown contracts、URL-driven detail shell、homepage click-through wiring、detail GET endpoint、query-service drilldown helper、frontend/backend/SQL verification.
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-drilldown-and-release-hardening/05-RESEARCH.md
@.planning/phases/05-drilldown-and-release-hardening/05-VALIDATION.md
@.planning/phases/02-dashboard-overview/02-dashboard-overview-01-SUMMARY.md
@.planning/phases/04-realtime-evaluation-workflow/04-realtime-evaluation-workflow-03-SUMMARY.md
@frontend/src/app/dashboard-overview-page.tsx
@frontend/src/lib/analytics-filters.ts
@frontend/src/components/distribution-section.tsx
@frontend/src/components/disease-insights-section.tsx
@src/api/http_dashboard_server.py
@src/domain/analytics/query_service.py

<interfaces>
Reuse the existing dashboard filter model directly:
```ts
export interface DashboardFilters {
  asOfDate: string;
  rangeKey: DashboardRangeKey;
  startDate?: string;
  endDate?: string;
  departmentIds?: string[];
  departmentTypeIds?: string[];
  doctorIds?: string[];
  diseaseIds?: string[];
}
```

Plan 05-01 should establish a contract shaped like:
```ts
export interface DrilldownIntent {
  dimension: 'department' | 'doctor' | 'disease';
  dimensionLabel: string;
  dimensionValue: string;
  sourceModule: 'distribution' | 'disease_insights' | 'overview';
  filters: DashboardFilters;
}

export interface ProblemCaseRow {
  encounter_id: string;
  case_id: string;
  patient_name: string;
  department_name: string;
  doctor_name: string;
  disease_name: string;
  evaluation_id: string | null;
  diagnosis_basis_incomplete: boolean;
  missing_diagnosis: boolean;
  triggered_at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define URL-driven drilldown contracts and homepage navigation entrypoints</name>
  <files>frontend/src/app/dashboard-overview-page.tsx, frontend/src/app/manager-drilldown-page.tsx, frontend/src/app/__tests__/manager-drilldown-page.test.tsx, frontend/src/components/distribution-section.tsx, frontend/src/components/disease-insights-section.tsx, frontend/src/components/__tests__/distribution-section.test.tsx, frontend/src/components/__tests__/disease-insights-section.test.tsx, frontend/src/lib/manager-drilldown-contracts.ts, frontend/src/components/manager-drilldown-header.tsx, frontend/src/components/drilldown-filter-summary.tsx</files>
  <action>Implement the manager-side navigation boundary per the Phase 5 research constraints. Add `frontend/src/lib/manager-drilldown-contracts.ts` with explicit `DrilldownIntent`, request, response, and dimension types that reuse `DashboardFilters` instead of inventing a second filter model. Add `frontend/src/app/manager-drilldown-page.tsx` as a dedicated manager detail shell whose state is derived from URL search parameters; refresh and copy-paste must preserve the chosen dimension and current homepage filter values. Update `frontend/src/app/dashboard-overview-page.tsx` to mount either the existing homepage or the new manager drilldown shell from URL state without taking ownership of the doctor embed surface. Extend `frontend/src/components/distribution-section.tsx` and `frontend/src/components/disease-insights-section.tsx` so they emit drilldown navigation intents for department/disease selections per `OPER-01`, while keeping current filter semantics intact. Add focused tests proving click-through behavior and drilldown page restoration from URL search params. Do not implement ephemeral local-state-only drilldown navigation.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/__tests__/distribution-section.test.tsx src/components/__tests__/disease-insights-section.test.tsx src/app/__tests__/manager-drilldown-page.test.tsx</automated>
  </verify>
  <done>管理端首页已经具备可分享、可刷新的 URL 化下钻入口，满足 `OPER-01` 的前端导航边界。</done>
</task>

<task type="auto">
  <name>Task 2: Add the problem-case detail endpoint and backend query wiring</name>
  <files>src/api/http_dashboard_server.py, src/api/http_dashboard_drilldown.py, src/api/analytics/drilldown_smoke.py, src/domain/analytics/contracts.py, src/domain/analytics/query_service.py, sql/tests/oper_01_problem_drilldown.sql, sql/tests/run-phase-01.sh, frontend/src/lib/manager-drilldown-api.ts, frontend/src/components/problem-case-table.tsx, frontend/src/app/manager-drilldown-page.tsx</files>
  <action>Implement the backend detail path that powers the drilldown shell from Task 1. Add `src/api/http_dashboard_drilldown.py` and register it from `src/api/http_dashboard_server.py` as a GET analytics route dedicated to manager drilldown. Extend `src/domain/analytics/contracts.py` and `src/domain/analytics/query_service.py` with one canonical case-row query helper that uses existing analytics filter semantics and Phase 4 persisted evaluation data rather than re-deriving metrics in the frontend. Add `frontend/src/lib/manager-drilldown-api.ts` and wire `frontend/src/app/manager-drilldown-page.tsx` to fetch the detail payload and render it through `frontend/src/components/problem-case-table.tsx`, plus lightweight header/filter-summary components. Add `src/api/analytics/drilldown_smoke.py` to prove the handler/query path returns the expected dimension-scoped rows, and add `sql/tests/oper_01_problem_drilldown.sql` with fixture-backed assertions for department/doctor/disease drilldown semantics. Keep screen rows and future export rows sourced from the same backend query helper so 05-02 can reuse it without semantic drift.</action>
  <verify>
    <automated>python3 -m py_compile src/api/http_dashboard_drilldown.py src/api/analytics/drilldown_smoke.py src/domain/analytics/query_service.py && python3 src/api/analytics/drilldown_smoke.py && export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh oper_01_problem_drilldown</automated>
  </verify>
  <done>管理者已经可以从首页入口进入真实病例明细，并由后端统一保证筛选语义和 Phase 4 持久化数据复用。</done>
</task>

</tasks>

<verification>
执行 `cd frontend && npm test -- --run src/components/__tests__/distribution-section.test.tsx src/components/__tests__/disease-insights-section.test.tsx src/app/__tests__/manager-drilldown-page.test.tsx`，再执行 `python3 -m py_compile src/api/http_dashboard_drilldown.py src/api/analytics/drilldown_smoke.py src/domain/analytics/query_service.py && python3 src/api/analytics/drilldown_smoke.py`，最后执行 `export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh oper_01_problem_drilldown`。
</verification>

<success_criteria>
`OPER-01` 对应的首页到病例明细下钻链路可被自动验证，且 URL、后端筛选语义、Phase 4 数据复用三个关键连接都已锁定。
</success_criteria>

<output>
After completion, create `.planning/phases/05-drilldown-and-release-hardening/05-drilldown-and-release-hardening-01-SUMMARY.md`
</output>
