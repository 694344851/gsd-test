---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/tests/run-phase-01.sh
  - sql/tests/fixtures/phase_01_seed.sql
  - sql/tests/data_01_mapping.sql
  - docs/data-source-inventory.md
  - docs/data-field-mapping.md
  - sql/staging/001_phase1_source_contracts.sql
  - sql/migrations/001_phase1_foundation.sql
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - "统一数据模型可以被重复加载与断言验证，且事实表、维表和病例级语义在重复导入后仍保持无漂移。"
    - "门诊、诊断和评估三类源数据的接入边界、键策略和字段映射被明确成可执行的集成层合同。"
    - "门诊就诊事实、评估事件事实和基础维表被明确分开建模。"
    - "模型层同时保留按就诊记录统计的默认门诊量和按患者去重统计的备用门诊量。"
    - "病例级语义层能把未触发、评估成功、评估失败或超时区分开，并为后续聚合提供稳定输入。"
  artifacts:
    - path: "sql/tests/run-phase-01.sh"
      provides: "Phase 1 SQL test harness"
      min_lines: 20
    - path: "sql/tests/fixtures/phase_01_seed.sql"
      provides: "Stable seed data for encounter/evaluation states"
      contains: "enc-001"
    - path: "docs/data-source-inventory.md"
      provides: "Source inventory and join-key validation rules for outpatient, diagnosis, and evaluation inputs"
      contains: "src_outpatient_encounters"
    - path: "docs/data-field-mapping.md"
      provides: "Field-mapping matrix from source-shaped inputs into staging/fact/dimension columns"
      contains: "source_column"
    - path: "sql/staging/001_phase1_source_contracts.sql"
      provides: "Staging views/contracts that normalize source-shaped outpatient, diagnosis, and evaluation inputs"
      contains: "stg_outpatient_encounter_source"
    - path: "sql/migrations/001_phase1_foundation.sql"
      provides: "Analytics schema, dimensions, facts, case-level semantic view, and outpatient volume semantic view"
      contains: "analytics.mart_outpatient_volume"
    - path: "sql/tests/data_01_mapping.sql"
      provides: "Schema and mapping assertions"
      contains: "analytics.fact_outpatient_encounter"
  key_links:
    - from: "sql/tests/run-phase-01.sh"
      to: "sql/tests/fixtures/phase_01_seed.sql"
      via: "psql -f fixture load before assertion selection"
      pattern: "phase_01_seed.sql"
    - from: "sql/migrations/001_phase1_foundation.sql"
      to: "analytics.mart_case_evaluation"
      via: "window function selecting latest successful evaluation"
      pattern: "row_number\\(\\) over"
    - from: "sql/migrations/001_phase1_foundation.sql"
      to: "analytics.mart_outpatient_volume"
      via: "semantic layer keeps encounter-count and unique-patient-count together"
      pattern: "unique_patient_outpatient_count"
    - from: "sql/staging/001_phase1_source_contracts.sql"
      to: "sql/migrations/001_phase1_foundation.sql"
      via: "staging contracts feed analytics facts from source-shaped inputs"
      pattern: "stg_outpatient_encounter_source|stg_diagnosis_source|stg_evaluation_event_source"
---

<objective>
搭建 Phase 1 的数据库基础层和可执行验证基座，先把粒度、状态语义和测试入口锁死。

Purpose: 先解决 DATA-01 的事实表边界与病例级归档规则，避免后续指标和接口建立在漂移语义上。
Output: SQL migration、夹具数据、映射断言脚本、统一测试入口。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
@门诊诊鉴首页-(全栈).md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 1 SQL validation harness and fixture dataset</name>
  <read_first>
    .planning/phases/01-data-foundation/01-VALIDATION.md
    .planning/phases/01-data-foundation/01-CONTEXT.md
    sql/tests/run-phase-01.sh
    sql/tests/fixtures/phase_01_seed.sql
    docs/data-source-inventory.md
    docs/data-field-mapping.md
    门诊诊鉴首页-(全栈).md
  </read_first>
  <files>sql/tests/run-phase-01.sh, sql/tests/fixtures/phase_01_seed.sql, sql/tests/data_01_mapping.sql, docs/data-source-inventory.md, docs/data-field-mapping.md</files>
  <acceptance_criteria>
    - `sql/tests/run-phase-01.sh` contains `case "$1" in` and explicit branches for `data_01_mapping`, `data_02_aggregates`, `data_03_status_split`, `dash_01_default_window`.
    - `sql/tests/run-phase-01.sh` contains `psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f sql/tests/fixtures/phase_01_seed.sql`.
    - `sql/tests/run-phase-01.sh` is structured so each named branch executes exactly one assertion file after migrations and fixtures, keeping the smallest relevant assertion path focused on a single test target.
    - `sql/tests/fixtures/phase_01_seed.sql` inserts at least 4 encounters named `enc-001`, `enc-002`, `enc-003`, `enc-004`.
    - `sql/tests/fixtures/phase_01_seed.sql` reuses at least one patient across multiple encounters so patient-deduplicated outpatient counts can differ from encounter counts.
    - `sql/tests/fixtures/phase_01_seed.sql` inserts evaluation rows covering `success`, `failed`, and `timeout`, while leaving one encounter with no evaluation row to represent `not_triggered`.
    - `docs/data-source-inventory.md` inventories source-shaped inputs for outpatient encounters, diagnoses, and evaluation events using explicit placeholders or source contracts named `src_outpatient_encounters`, `src_outpatient_diagnoses`, and `src_evaluation_events`.
    - `docs/data-source-inventory.md` contains a join-key validation section that states how `encounter_id` and `case_id` are used, and what to do with null keys and duplicate source rows.
    - `docs/data-field-mapping.md` contains a mapping matrix with columns `source_entity`, `source_column`, `target_layer`, `target_object`, `target_column`, and `transform_rule`.
    - `sql/tests/data_01_mapping.sql` contains assertions against staging/source contracts, `analytics.fact_outpatient_encounter`, `analytics.fact_evaluation_event`, `analytics.mart_case_evaluation`, and `analytics.mart_outpatient_volume`.
    - `sql/tests/data_01_mapping.sql` contains an assertion on `unique_patient_outpatient_count`.
    - `sql/tests/data_01_mapping.sql` contains an assertion or check query that fails if source rows with null `encounter_id`/`case_id` are loaded into fact tables without the documented handling rule.
  </acceptance_criteria>
  <action>Per D-01, D-02, D-03, D-04, D-05 and the Wave 0 requirements in `01-VALIDATION.md`, create a shell runner at `sql/tests/run-phase-01.sh` that accepts exactly one positional test name (`data_01_mapping`, `data_02_aggregates`, `data_03_status_split`, `dash_01_default_window`), applies `sql/migrations/*.sql` in lexical order, loads `sql/tests/fixtures/phase_01_seed.sql`, then executes only the selected assertion file with `psql "$DATABASE_URL" -v ON_ERROR_STOP=1`. The intent is a smallest relevant assertion path that can stay under roughly 30 seconds when running a single named SQL test. Create fixture rows that make later assertions deterministic: `enc-001` and `enc-004` share `patient_id = 'pat-001'` so encounter-count and patient-deduplicated-count differ; `enc-001` has two evaluation attempts with the latest successful row flagged `diagnosis_basis_incomplete=true` and `missing_diagnosis=false`; `enc-002` has one `failed` event and no success row; `enc-003` has one `timeout` event and no success row; `enc-004` has no evaluation row at all. Include matching dimension rows for `dept-ob`, `dept-gy`, `dept-type-ob`, `doctor-001`, `doctor-002`, `disease-a`, `disease-b`. Add `docs/data-source-inventory.md` that inventories source-shaped contracts for outpatient encounters, diagnoses, and evaluation events using explicit placeholder objects `src_outpatient_encounters`, `src_outpatient_diagnoses`, and `src_evaluation_events`; document the join strategy `encounter_id` as primary visit key, `case_id` as fallback correlation key when evaluation payloads omit `encounter_id`; and define null/duplication handling as `drop and log rows with both keys null`, `deduplicate outpatient source by latest extract_ts per encounter_id`, `deduplicate diagnosis source by latest diagnosis_version per encounter_id + disease_code`, and `deduplicate evaluation source by latest triggered_at per evaluation_id`. Add `docs/data-field-mapping.md` with a row-by-row mapping matrix from those source-shaped inputs into staging objects, facts, and dimensions. Create `sql/tests/data_01_mapping.sql` with assertion queries that fail if any required source inventory artifact, staging contract, table/view is missing, if `enc-001` does not resolve to the latest successful evaluation, if `enc-004` is not classified as `not_triggered` in the semantic layer, if null-key handling is bypassed, or if `analytics.mart_outpatient_volume` does not preserve both `encounter_outpatient_count = 4` and `unique_patient_outpatient_count = 3` for the full seeded window.</action>
  <verify>
    <automated>test -x sql/tests/run-phase-01.sh && rg -n 'data_01_mapping|data_02_aggregates|data_03_status_split|dash_01_default_window|phase_01_seed.sql|unique_patient_outpatient_count' sql/tests/run-phase-01.sh sql/tests/fixtures/phase_01_seed.sql sql/tests/data_01_mapping.sql</automated>
  </verify>
  <done>执行脚本能在同一命令里完成 migration、fixture load 和 `data_01_mapping` 断言，且夹具已覆盖成功、失败、超时、未触发四类病例状态。</done>
</task>

<task type="auto">
  <name>Task 2: Implement the analytics schema, dual facts, dimensions, and case-level semantic view</name>
  <read_first>
    .planning/phases/01-data-foundation/01-CONTEXT.md
    .planning/phases/01-data-foundation/01-RESEARCH.md
    docs/data-source-inventory.md
    docs/data-field-mapping.md
    sql/staging/001_phase1_source_contracts.sql
    sql/migrations/001_phase1_foundation.sql
    sql/tests/fixtures/phase_01_seed.sql
    sql/tests/data_01_mapping.sql
  </read_first>
  <files>sql/staging/001_phase1_source_contracts.sql, sql/migrations/001_phase1_foundation.sql</files>
  <acceptance_criteria>
    - `sql/staging/001_phase1_source_contracts.sql` defines source-shaped staging objects named `stg_outpatient_encounter_source`, `stg_outpatient_diagnosis_source`, and `stg_evaluation_event_source`.
    - The staging SQL contains explicit key-normalization logic for `encounter_id` and `case_id`, including a fallback path and a rejection path for rows with both keys null.
    - The staging SQL contains deduplication logic for duplicate outpatient, diagnosis, and evaluation source rows using deterministic ordering columns such as `extract_ts`, `diagnosis_version`, or `triggered_at`.
    - `sql/migrations/001_phase1_foundation.sql` contains `create schema if not exists analytics;`.
    - The migration defines tables `analytics.dim_department_type`, `analytics.dim_department`, `analytics.dim_doctor`, `analytics.dim_disease`, `analytics.fact_outpatient_encounter`, and `analytics.fact_evaluation_event`.
    - `analytics.fact_outpatient_encounter` contains columns `encounter_id`, `case_id`, `patient_id`, `encounter_at`, `shift_code`, `department_id`, `department_type_id`, `doctor_id`, `disease_id`, and `is_diagnosis_present`.
    - `analytics.fact_evaluation_event` contains columns `evaluation_id`, `encounter_id`, `triggered_at`, `status`, `diagnosis_basis_incomplete`, `missing_diagnosis`, and `quality_index_score`.
    - The migration defines `analytics.mart_outpatient_volume` with columns `bucket_start`, `bucket_grain`, `encounter_outpatient_count`, and `unique_patient_outpatient_count`.
    - The migration creates `analytics.mart_case_evaluation` with derived columns `evaluation_state`, `trigger_count`, `success_count`, `latest_success_evaluation_id`, `was_successfully_evaluated`, and uses `row_number() over (partition by encounter_id order by triggered_at desc, evaluation_id desc)`.
    - The analytics fact-load path reads from `stg_outpatient_encounter_source`, `stg_outpatient_diagnosis_source`, and `stg_evaluation_event_source` rather than assuming raw source tables can be queried directly.
  </acceptance_criteria>
  <action>Implement `sql/staging/001_phase1_source_contracts.sql` and `sql/migrations/001_phase1_foundation.sql` together as the DATA-01 integration layer plus target analytics schema. In `sql/staging/001_phase1_source_contracts.sql`, create source-shaped staging views or load contracts named `stg_outpatient_encounter_source`, `stg_outpatient_diagnosis_source`, and `stg_evaluation_event_source` that normalize upstream outpatient, diagnosis, and evaluation inputs even when the real upstream tables are still unknown in this greenfield repo. Use placeholder source objects named `src_outpatient_encounters`, `src_outpatient_diagnoses`, and `src_evaluation_events` as the assumed raw inputs. Implement key validation exactly as documented in the source inventory: `encounter_id` is the preferred join key, `case_id` is the fallback correlation key, rows with both keys null must be excluded from fact loads, duplicate outpatient rows are reduced to the latest `extract_ts` per `encounter_id`, duplicate diagnosis rows are reduced to the latest `diagnosis_version` per `encounter_id + disease_code`, and duplicate evaluation rows are reduced to the latest `triggered_at` per `evaluation_id`. In `sql/migrations/001_phase1_foundation.sql`, create schema `analytics`; dimension tables for department type, department, doctor, and disease; fact table `analytics.fact_outpatient_encounter` at one-row-per-encounter grain sourced from `stg_outpatient_encounter_source` and `stg_outpatient_diagnosis_source`; and fact table `analytics.fact_evaluation_event` at one-row-per-evaluation-attempt grain sourced from `stg_evaluation_event_source`. Store evaluation event statuses as `success`, `failed`, or `timeout`; derive the `not_triggered` case state in the semantic layer instead of persisting a fake event row, per D-02. Include `case_id`, `encounter_id`, and `patient_id` on encounter fact rows per D-01 and D-05 so patient-level deduplication remains available even though default outpatient volume is encounter-count. Preserve both encounter-level diagnosis presence and evaluation-level issue flags. Create a materialized view named `analytics.mart_case_evaluation` that left joins encounters to a ranked-success CTE, exposes `trigger_count` as total event attempts, `success_count` as successful attempts, `evaluation_state` as one of `not_triggered`, `success`, `failed`, `timeout`, and resolves the case-level flags from the latest successful evaluation only per D-03. Also create a semantic view or materialized view named `analytics.mart_outpatient_volume` that groups from `analytics.fact_outpatient_encounter` and exposes both `encounter_outpatient_count` as the default metric and `unique_patient_outpatient_count` as the preserved patient-deduplicated metric per D-05. Add indexes on `fact_outpatient_encounter(encounter_at)`, `fact_outpatient_encounter(patient_id, encounter_at)`, `fact_outpatient_encounter(department_id, doctor_id, disease_id)`, `fact_evaluation_event(encounter_id, triggered_at desc)`, and a partial index on `fact_evaluation_event(encounter_id, triggered_at desc) where status = 'success'`.</action>
  <verify>
    <automated>bash sql/tests/run-phase-01.sh data_01_mapping</automated>
  </verify>
  <done>数据库层已经稳定表达双事实模型、基础维表和病例级最新成功评估语义，`data_01_mapping` 断言通过。</done>
</task>

</tasks>

<verification>
Run `bash sql/tests/run-phase-01.sh data_01_mapping`. This is the intended smallest relevant assertion path for plan 01 and should stay focused on a single SQL assertion file. Confirm the migration creates all analytics objects, the fixture rows resolve to the intended case states, and the seeded window preserves both encounter-count and unique-patient-count outpatient metrics.
</verification>

<success_criteria>
`DATA-01` 对应的数据模型已经落地：执行者可重复加载 Phase 1 夹具，且双事实模型与病例级语义视图可被 SQL 断言自动验证。
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-data-foundation-01-SUMMARY.md`
</output>
