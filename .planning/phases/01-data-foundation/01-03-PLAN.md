---
phase: 01-data-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - scripts/query-pg.mjs
  - src/domain/analytics/contracts.py
  - src/domain/analytics/query_service.py
  - src/api/analytics/overview.py
  - src/api/analytics/trend.py
  - src/api/analytics/evaluation_split.py
  - sql/tests/data_03_status_split.sql
autonomous: true
requirements:
  - DATA-03
must_haves:
  truths:
    - "调用方可以分别获取概览汇总、趋势序列和已评估/未评估拆分结果。"
    - "已评估与未评估的划分不会把失败或超时错误地并入未触发。"
    - "查询服务会把时间范围、科室、科室类型、医生和病种筛选条件翻译并传入 SQL 语义层。"
    - "场景接口仅包装领域结果，不会改写基础语义字段。"
  artifacts:
    - path: "src/domain/analytics/contracts.py"
      provides: "Shared analytics Python dataclass contracts"
      exports: ["AnalyticsFilters", "OverviewSummaryRow", "TrendSeriesRow", "EvaluationSplitRow"]
    - path: "src/domain/analytics/query_service.py"
      provides: "Shared analytics Python query entry points"
      exports: ["get_overview_summary", "get_trend_series", "get_evaluation_split"]
    - path: "src/api/analytics/overview.py"
      provides: "Overview-facing response wrapper"
      exports: ["build_overview_response"]
    - path: "src/api/analytics/trend.py"
      provides: "Trend-facing response wrapper"
      exports: ["build_trend_response"]
    - path: "src/api/analytics/evaluation_split.py"
      provides: "Evaluated vs unevaluated response wrapper"
      exports: ["build_evaluation_split_response"]
    - path: "sql/tests/data_03_status_split.sql"
      provides: "Evaluation-state split assertions"
      contains: "not_triggered_count"
  key_links:
    - from: "src/domain/analytics/query_service.py"
      to: "src/domain/analytics/contracts.py"
      via: "dataclass and type imports define stable query signatures"
      pattern: "from \\.contracts import AnalyticsFilters, EvaluationSplitRow, OverviewSummaryRow, TrendSeriesRow"
    - from: "src/domain/analytics/query_service.py"
      to: "sql/migrations/002_phase1_metrics.sql"
      via: "query names mirror SQL objects and pass filter parameters through"
      pattern: "analytics\\.get_overview_summary|analytics\\.get_trend_series|department_ids|doctor_ids|disease_ids"
    - from: "src/api/analytics/overview.py"
      to: "src/domain/analytics/query_service.py"
      via: "wrapper delegates overview fetching to query service"
      pattern: "get_overview_summary\\("
    - from: "src/api/analytics/trend.py"
      to: "src/domain/analytics/query_service.py"
      via: "wrapper delegates trend fetching to query service"
      pattern: "get_trend_series\\("
    - from: "src/api/analytics/evaluation_split.py"
      to: "src/domain/analytics/query_service.py"
      via: "wrapper delegates evaluation split fetching to query service"
      pattern: "get_evaluation_split\\("
    - from: "src/domain/analytics/query_service.py"
      to: "analytics.mart_case_evaluation"
      via: "query service computes state counts from the case evaluation semantic layer"
      pattern: "mart_case_evaluation|failed_count|timeout_count|not_triggered_count"
---

<objective>
把底层聚合语义发布为可复用的查询服务和场景接口，完成已评估/未评估拆分能力。

Purpose: 解决 `DATA-03`，并把 Phase 1 的 SQL 语义包装成 Phase 2/3/4 可直接复用的 contract，而不是把业务规则散落在调用方。
Output: 领域查询服务、概览/趋势/状态拆分接口包装器、状态拆分断言脚本。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-01-PLAN.md
@.planning/phases/01-data-foundation/01-02-PLAN.md
@src/domain/analytics/contracts.py

<interfaces>
Implement these exported signatures in `src/domain/analytics/query_service.py`:

```python
from src.domain.analytics.contracts import (
    AnalyticsFilters,
    EvaluationSplitRow,
    OverviewSummaryRow,
    TrendSeriesRow,
)

def get_overview_summary(filters: AnalyticsFilters, *, executor=None, connection_string=None) -> OverviewSummaryRow: ...
def get_trend_series(filters: AnalyticsFilters, *, executor=None, connection_string=None) -> list[TrendSeriesRow]: ...
def get_evaluation_split(filters: AnalyticsFilters, *, executor=None, connection_string=None) -> EvaluationSplitRow: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the typed analytics query service over the SQL semantic layer</name>
  <read_first>
    src/domain/analytics/contracts.py
    src/domain/analytics/query_service.py
    sql/migrations/001_phase1_foundation.sql
    sql/migrations/002_phase1_metrics.sql
    .planning/phases/01-data-foundation/01-CONTEXT.md
    .planning/phases/01-data-foundation/01-RESEARCH.md
  </read_first>
  <files>scripts/query-pg.mjs, src/domain/analytics/contracts.py, src/domain/analytics/query_service.py</files>
  <acceptance_criteria>
    - `src/domain/analytics/contracts.py` defines `AnalyticsFilters`, `OverviewSummaryRow`, `TrendSeriesRow`, and `EvaluationSplitRow`.
    - `src/domain/analytics/query_service.py` exports `get_overview_summary`, `get_trend_series`, and `get_evaluation_split`.
    - `get_evaluation_split` returns fields `outpatient_count`, `not_triggered_count`, `success_count`, `failed_count`, and `timeout_count`.
    - `src/domain/analytics/query_service.py` contains `analytics.get_overview_summary`.
    - `src/domain/analytics/query_service.py` contains `analytics.get_trend_series`.
    - `src/domain/analytics/query_service.py` contains `analytics.mart_case_evaluation` for the evaluation split query.
    - `src/domain/analytics/query_service.py` passes `as_of_date`, `range_key`, `start_date`, `end_date`, `department_ids`, `department_type_ids`, `doctor_ids`, and `disease_ids` into overview/trend SQL calls instead of dropping any supported filter.
    - `src/domain/analytics/query_service.py` consumes SQL outputs directly and does not recompute metric semantics in wrappers.
    - `src/domain/analytics/query_service.py` does not expose or remap `qualityIndexScore` in Phase 1 outward query results.
    - The file does not contain UI labels such as `cardTitle`, `legend`, or `tooltip`.
  </acceptance_criteria>
  <action>Implement `src/domain/analytics/query_service.py` as the single Python domain entry point for Phase 1 query semantics, using the exact signatures from the interfaces block and dataclass contracts from `src/domain/analytics/contracts.py`. Expose `get_overview_summary(filters)`, `get_trend_series(filters)`, and `get_evaluation_split(filters)`. `get_evaluation_split` must classify records exactly as follows per D-02 and D-14: `not_triggered_count` counts rows where `evaluation_state = 'not_triggered'`; `success_count` counts rows where `evaluation_state = 'success'`; `failed_count` counts rows where `evaluation_state = 'failed'`; `timeout_count` counts rows where `evaluation_state = 'timeout'`. Do not collapse `failed` or `timeout` into `not_triggered_count`. Keep the contract boundary consistent with Plan 02 by delegating metric computation to `analytics.get_overview_summary`, `analytics.get_trend_series`, and `analytics.mart_case_evaluation`. Honor D-08 by not adding `qualityIndexScore` back into Python return shapes or wrapper payloads. Per D-15, translate every supported filter into SQL-call parameters: pass `as_of_date`, `range_key`, `start_date`, `end_date`, `department_ids`, `department_type_ids`, `doctor_ids`, and `disease_ids` through to overview/trend SQL objects, and apply the same dimension filters when computing evaluation-split counts. Keep the module domain-oriented and framework-agnostic. Use a thin Node `pg` bridge script only as the database execution adapter; business semantics stay in Python and SQL.</action>
  <verify>
    <automated>python3 -m py_compile src/domain/analytics/contracts.py src/domain/analytics/query_service.py src/api/analytics/overview.py src/api/analytics/trend.py src/api/analytics/evaluation_split.py && rg -n "def get_overview_summary|def get_trend_series|def get_evaluation_split|analytics\\.get_overview_summary|analytics\\.get_trend_series|analytics\\.mart_case_evaluation|as_of_date|range_key|start_date|end_date|department_ids|department_type_ids|doctor_ids|disease_ids" src/domain/analytics/query_service.py && ! rg -n "qualityIndexScore" src/domain/analytics/query_service.py src/api/analytics/*.py</automated>
  </verify>
  <done>领域层已有稳定查询入口，任何后续页面或任务都能在不重写业务 SQL 的前提下获取概览、趋势和状态拆分结果。</done>
</task>

<task type="auto">
  <name>Task 2: Add scenario-facing overview, trend, and evaluation-split wrappers plus status-split assertions</name>
  <read_first>
    src/domain/analytics/contracts.py
    src/domain/analytics/query_service.py
    src/api/analytics/overview.py
    src/api/analytics/trend.py
    src/api/analytics/evaluation_split.py
    sql/tests/data_03_status_split.sql
    .planning/phases/01-data-foundation/01-CONTEXT.md
  </read_first>
  <files>src/api/analytics/overview.py, src/api/analytics/trend.py, src/api/analytics/evaluation_split.py, sql/tests/data_03_status_split.sql</files>
  <acceptance_criteria>
    - `src/api/analytics/overview.py` exports `build_overview_response`.
    - `src/api/analytics/trend.py` exports `build_trend_response`.
    - `src/api/analytics/evaluation_split.py` exports `build_evaluation_split_response`.
    - `src/api/analytics/overview.py` contains a call to `get_overview_summary(` and returns an envelope containing `filters` and `summary`.
    - `src/api/analytics/trend.py` contains a call to `get_trend_series(` and returns an envelope containing `filters` and `series`.
    - `src/api/analytics/evaluation_split.py` contains a call to `get_evaluation_split(` and returns an envelope containing `filters` and `split`.
    - `src/api/analytics/overview.py`, `src/api/analytics/trend.py`, and `src/api/analytics/evaluation_split.py` do not expose `qualityIndexScore`.
    - `src/api/analytics/evaluation_split.py` maps output fields `not_triggered_count`, `success_count`, `failed_count`, and `timeout_count` without collapsing them to a single evaluated field.
    - `sql/tests/data_03_status_split.sql` contains assertions for `not_triggered_count`, `success_count`, `failed_count`, and `timeout_count`.
    - The assertion file fails if `failed` or `timeout` rows are counted as `not_triggered`.
  </acceptance_criteria>
  <action>Per D-13, D-14, D-15 and D-16, create three thin Python scenario wrappers: `src/api/analytics/overview.py` exporting `build_overview_response(filters)` that calls `get_overview_summary(filters)` and returns `{ "filters": ..., "summary": ... }`; `src/api/analytics/trend.py` exporting `build_trend_response(filters)` that calls `get_trend_series(filters)` and returns `{ "filters": ..., "series": ... }`; and `src/api/analytics/evaluation_split.py` exporting `build_evaluation_split_response(filters)` that calls `get_evaluation_split(filters)` and returns `{ "filters": ..., "split": ... }`. These wrappers must preserve the field names of the Python domain payloads and must not recompute ratios, reintroduce `qualityIndexScore`, or collapse status buckets. Keep the SQL assertion `sql/tests/data_03_status_split.sql` aligned with the shared fixture dataset so it proves `not_triggered_count = 1`, `success_count = 1`, `failed_count = 1`, and `timeout_count = 1` for the seed cases `enc-001` through `enc-004`, and fails if failed/timeout are folded into the unevaluated bucket.</action>
  <verify>
    <automated>bash sql/tests/run-phase-01.sh data_03_status_split && python3 -m py_compile src/api/analytics/overview.py src/api/analytics/trend.py src/api/analytics/evaluation_split.py && rg -n "def build_overview_response|get_overview_summary\\(|filters|summary|def build_trend_response|get_trend_series\\(|series|def build_evaluation_split_response|get_evaluation_split\\(|split" src/api/analytics/*.py && ! rg -n "qualityIndexScore" src/api/analytics/*.py</automated>
  </verify>
  <done>面向场景的基础接口已经存在，且已评估/未评估拆分语义被自动断言锁死，后续首页和下钻可以直接复用。</done>
</task>

</tasks>

<verification>
Run `bash sql/tests/run-phase-01.sh data_03_status_split` for the data-path check, then run the focused Python `py_compile` + `rg` wiring verification from Task 1 for a sub-30-second semantic-layer proof. Confirm the API wrapper files export exactly the three Python response-builder functions and do not flatten failure/timeout into `not_triggered_count`.
</verification>

<success_criteria>
`DATA-03` 对应的状态拆分与基础查询接口已经具备：Python 领域服务提供稳定 typed query entry points，场景层可直接消费且语义未被篡改。
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-data-foundation-03-SUMMARY.md`
</output>
