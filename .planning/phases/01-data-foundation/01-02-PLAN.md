---
phase: 01-data-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/domain/analytics/contracts.ts
  - sql/migrations/002_phase1_metrics.sql
  - sql/tests/data_02_aggregates.sql
  - sql/tests/dash_01_default_window.sql
autonomous: true
requirements:
  - DATA-02
  - DASH-01
must_haves:
  truths:
    - "默认时间范围可以稳定返回以昨日为截止、近三个月共 13 个周单元的起止日期和粒度。"
    - "门诊量、触发数、成功评估数、诊断依据不完整比例、缺失诊断比例可以按统一分母规则聚合。"
    - "默认门诊量按就诊记录数统计，同时保留按患者去重的门诊量供后续分析复用。"
    - "概览与趋势查询会在 SQL 语义层实际应用时间范围、科室、科室类型、医生和病种筛选条件。"
    - "整体诊断质量指数在 Phase 1 只以内置配置占位方式保留，默认不进入稳定对外查询结果。"
    - "触发数与成功评估数在语义层被明确拆分，对外默认评估病例数使用成功评估数。"
    - "两个问题比例同时保留按成功评估病例数和按全部门诊量计算的分母版本，对外默认使用成功评估病例数分母。"
    - "同一时间窗口下，概览和趋势查询共享同一套时间桶和口径定义。"
  artifacts:
    - path: "src/domain/analytics/contracts.ts"
      provides: "Stable analytics query contracts"
      exports: ["AnalyticsTimeRangeKey", "AnalyticsFilters", "OverviewSummaryRow", "TrendSeriesRow"]
    - path: "sql/migrations/002_phase1_metrics.sql"
      provides: "Default window logic, aggregate SQL functions, and internal quality-index placeholder config"
      contains: "create or replace function analytics.resolve_time_window"
    - path: "sql/tests/data_02_aggregates.sql"
      provides: "Aggregate metric assertions"
      contains: "unique_patient_outpatient_count"
    - path: "sql/tests/dash_01_default_window.sql"
      provides: "Default range assertions"
      contains: "2026-03-24"
  key_links:
    - from: "sql/migrations/002_phase1_metrics.sql"
      to: "analytics.mart_case_evaluation"
      via: "aggregate functions read case-level flags"
      pattern: "from analytics\\.mart_case_evaluation"
    - from: "src/domain/analytics/contracts.ts"
      to: "sql/migrations/002_phase1_metrics.sql"
      via: "contract field names mirror SQL outputs"
      pattern: "successEvaluatedCount|bucketLabel|departmentIds|doctorIds"
---

<objective>
固化默认时间窗口和核心指标聚合逻辑，确保概览与趋势共享同一时间与分母规则。

Purpose: 解决 `DATA-02` 和 `DASH-01`，把 PRD 的默认“近三个月”与“截止昨日”落到可复用 SQL 语义层。
Output: 查询 contract、时间窗口函数、聚合 SQL、默认窗口与指标断言脚本。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
@.planning/phases/01-data-foundation/01-01-PLAN.md
@门诊诊鉴首页-(全栈).md

<interfaces>
Create these exported contracts in `src/domain/analytics/contracts.ts` so downstream plan 03 can implement against them directly:

```ts
export type AnalyticsTimeRangeKey =
  | 'last_7_days'
  | 'last_30_days'
  | 'last_3_months'
  | 'last_6_months'
  | 'last_12_months'
  | 'previous_week'
  | 'previous_month'
  | 'previous_quarter'
  | 'previous_year'
  | 'custom';

export interface AnalyticsFilters {
  asOfDate: string;
  rangeKey: AnalyticsTimeRangeKey;
  startDate?: string;
  endDate?: string;
  departmentIds?: string[];
  departmentTypeIds?: string[];
  doctorIds?: string[];
  diseaseIds?: string[];
}

export interface OverviewSummaryRow {
  outpatientCount: number;
  uniquePatientOutpatientCount: number;
  triggeredEvaluationCount: number;
  successEvaluatedCount: number;
  diagnosisBasisIncompleteRateBySuccess: number | null;
  diagnosisBasisIncompleteRateByEncounter: number | null;
  missingDiagnosisRateBySuccess: number | null;
  missingDiagnosisRateByEncounter: number | null;
  rangeStartDate: string;
  rangeEndDate: string;
  bucketGrain: 'shift' | 'day' | 'week' | 'month';
}

export interface TrendSeriesRow extends OverviewSummaryRow {
  bucketStart: string;
  bucketEnd: string;
  bucketLabel: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define analytics query contracts for filters, overview, and trend rows</name>
  <read_first>
    .planning/phases/01-data-foundation/01-CONTEXT.md
    .planning/phases/01-data-foundation/01-RESEARCH.md
    src/domain/analytics/contracts.ts
    .planning/phases/01-data-foundation/01-01-PLAN.md
  </read_first>
  <files>src/domain/analytics/contracts.ts</files>
  <acceptance_criteria>
    - `src/domain/analytics/contracts.ts` exports `AnalyticsTimeRangeKey`, `AnalyticsFilters`, `OverviewSummaryRow`, and `TrendSeriesRow`.
    - `AnalyticsTimeRangeKey` includes exactly `last_7_days`, `last_30_days`, `last_3_months`, `last_6_months`, `last_12_months`, `previous_week`, `previous_month`, `previous_quarter`, `previous_year`, and `custom`.
    - `AnalyticsFilters` contains `asOfDate`, `rangeKey`, `startDate`, `endDate`, `departmentIds`, `departmentTypeIds`, `doctorIds`, and `diseaseIds`.
    - `OverviewSummaryRow` contains `outpatientCount`, `uniquePatientOutpatientCount`, both success-denominator and encounter-denominator ratio fields, and a `bucketGrain` union of `shift | day | week | month`.
    - `src/domain/analytics/contracts.ts` does not contain `qualityIndexScore`.
    - The contract file documents that `departmentIds`, `departmentTypeIds`, `doctorIds`, and `diseaseIds` are SQL-applied filters rather than UI-only optional fields.
  </acceptance_criteria>
  <action>Per D-05, D-08, D-10, D-12, D-15 and D-16, create `src/domain/analytics/contracts.ts` exporting the exact TypeScript types shown in the interfaces block above, with one addition: `OverviewSummaryRow` and therefore `TrendSeriesRow` must include `uniquePatientOutpatientCount: number` immediately after `outpatientCount`. `outpatientCount` remains the default encounter-count metric per D-05; `uniquePatientOutpatientCount` preserves the patient-deduplicated metric in the domain contract for later reuse. Use ISO date strings for `asOfDate`, `rangeStartDate`, `rangeEndDate`, `bucketStart`, and `bucketEnd`. Do not include `qualityIndexScore` in `OverviewSummaryRow` or `TrendSeriesRow`; D-08 locks it as an internal config-backed placeholder that is not a stable outward metric in Phase 1. Treat `departmentIds`, `departmentTypeIds`, `doctorIds`, and `diseaseIds` as real backend filters that must be translated into SQL predicates by downstream query objects, not as passive typed placeholders. Do not add UI-specific fields such as card titles, colors, chart series names, or tooltip text; this contract is the stable domain layer that later API wrappers will map into scenario-specific responses.</action>
  <verify>
    <automated>rg -n "export (type|interface) (AnalyticsTimeRangeKey|AnalyticsFilters|OverviewSummaryRow|TrendSeriesRow)" src/domain/analytics/contracts.ts</automated>
  </verify>
  <done>下游实现已经有稳定 contract 可依赖，不需要再从 PRD 或上下文中猜测筛选键、时间范围键和聚合结果字段。</done>
</task>

<task type="auto">
  <name>Task 2: Implement default time-window logic and aggregate SQL for overview and trend queries</name>
  <read_first>
    .planning/phases/01-data-foundation/01-CONTEXT.md
    门诊诊鉴首页-(全栈).md
    sql/migrations/001_phase1_foundation.sql
    sql/migrations/002_phase1_metrics.sql
    sql/tests/data_02_aggregates.sql
    sql/tests/dash_01_default_window.sql
    src/domain/analytics/contracts.ts
  </read_first>
  <files>sql/migrations/002_phase1_metrics.sql, sql/tests/data_02_aggregates.sql, sql/tests/dash_01_default_window.sql</files>
  <acceptance_criteria>
    - `sql/migrations/002_phase1_metrics.sql` defines `create or replace function analytics.resolve_time_window(range_key text, as_of_date date, custom_start date default null, custom_end date default null)`.
    - The function maps `last_7_days` and `previous_week` to `shift`, `last_30_days` and `previous_month` to `day`, `last_3_months`, `last_6_months`, and `previous_quarter` to `week`, and `last_12_months` and `previous_year` to `month`.
    - For `shift` grain, `sql/migrations/002_phase1_metrics.sql` defines deterministic bucket boundaries `[00:00,12:00)` as `AM` and `[12:00,24:00)` as `PM`, and trend labels include both date and half-day marker such as `2026-03-20 AM`.
    - The function returns `range_start_date`, `range_end_date`, `bucket_grain`, and does not use `current_date`.
    - The migration defines a reusable aggregate object named either `analytics.overview_summary` or `analytics.get_overview_summary`, and a trend object named either `analytics.trend_series` or `analytics.get_trend_series`.
    - The overview and trend aggregate outputs include both `outpatient_count` and `unique_patient_outpatient_count`, with `outpatient_count` sourced from encounter rows and `unique_patient_outpatient_count` sourced from `count(distinct patient_id)` or `analytics.mart_outpatient_volume`.
    - The overview and trend aggregate outputs explicitly include separate `triggeredEvaluationCount` and `successEvaluatedCount` fields, and documentation/comments in the SQL object state that the default homepage evaluated-case metric is `successEvaluatedCount` per D-06.
    - `sql/tests/dash_01_default_window.sql` asserts that `as_of_date = DATE '2026-03-24'` with `last_3_months` resolves to `range_start_date = DATE '2025-12-22'`, `range_end_date = DATE '2026-03-23'`, and `bucket_grain = 'week'`.
    - `sql/tests/dash_01_default_window.sql` contains a targeted shift-bucket assertion proving that timestamps from `00:00:00` up to but not including `12:00:00` map to `AM`, and timestamps from `12:00:00` up to but not including `24:00:00` map to `PM`.
    - `sql/tests/data_02_aggregates.sql` asserts metric outputs for the fixture data, including `outpatient_count = 4`, `unique_patient_outpatient_count = 3`, separate `triggeredEvaluationCount` and `successEvaluatedCount`, `diagnosisBasisIncompleteRateBySuccess`, `diagnosisBasisIncompleteRateByEncounter`, `missingDiagnosisRateBySuccess`, and `missingDiagnosisRateByEncounter`.
    - The SQL aggregate objects alias outputs directly to camelCase contract names including `outpatientCount`, `uniquePatientOutpatientCount`, `triggeredEvaluationCount`, `successEvaluatedCount`, `rangeStartDate`, `rangeEndDate`, `bucketGrain`, `bucketStart`, `bucketEnd`, and `bucketLabel`.
    - The SQL aggregate objects apply filter predicates for `departmentIds`, `departmentTypeIds`, `doctorIds`, and `diseaseIds`, using nullable array parameters or equivalent parameterized SQL conditions instead of ignoring empty filters.
    - `sql/migrations/002_phase1_metrics.sql` contains an internal placeholder config artifact for the quality index, such as `analytics.metric_placeholder_config` or `analytics.get_quality_index_placeholder_config`, with an explicit disabled flag for Phase 1.
    - The overview and trend SQL aggregate outputs do not expose `qualityIndexScore`.
    - The SQL semantic layer preserves both denominator variants for each quality ratio: outward/default rows expose `diagnosisBasisIncompleteRateBySuccess` and `missingDiagnosisRateBySuccess`, while the all-outpatient denominator variants remain available as `diagnosisBasisIncompleteRateByEncounter` and `missingDiagnosisRateByEncounter` for internal or downstream semantic reuse per D-07.
    - `sql/tests/data_02_aggregates.sql` contains focused assertions proving at least one filter changes output, such as `departmentIds = ARRAY['dept-ob']` returning fewer rows than the unfiltered query and `doctorIds = ARRAY['doctor-001']` restricting the aggregate.
    - `sql/tests/data_02_aggregates.sql` contains an assertion that the Phase 1 overview/trend output columns do not include `qualityIndexScore`, while the internal placeholder config remains disabled.
  </acceptance_criteria>
  <action>Implement `sql/migrations/002_phase1_metrics.sql` with an explicit date-driven window function and aggregate queries, using only PostgreSQL 14-compatible SQL. Per D-09 and the PRD, `last_3_months` must mean "the 12 weeks before the week containing yesterday, plus the week containing yesterday" for a total of 13 weekly buckets; for `as_of_date = DATE '2026-03-24'`, that means `range_start_date = DATE '2025-12-22'` and `range_end_date = DATE '2026-03-23'`. Per D-10, every function must accept `as_of_date` and must not reference `CURRENT_DATE`. Per D-11, generate historical buckets only within the resolved closed interval and do not emit future buckets. Per D-12, implement grain mapping exactly as follows: `last_7_days` and `previous_week` => `shift`; `last_30_days` and `previous_month` => `day`; `last_3_months`, `last_6_months`, `previous_quarter` => `week`; `last_12_months`, `previous_year` => `month`; `custom` => `day` for spans under 30 days, `week` for spans under 90 days, `month` for spans under 365 days, otherwise `month`. For `shift` grain, define bucket encoding deterministically as `[00:00,12:00)` = `AM` and `[12:00,24:00)` = `PM`; compute bucket boundaries in hospital local time; and emit trend labels in the concrete format `YYYY-MM-DD AM` or `YYYY-MM-DD PM`. Build overview and trend aggregates from `analytics.fact_outpatient_encounter` joined to `analytics.mart_case_evaluation`, while preserving D-05 exactly: `outpatientCount` is the default encounter-count metric, and `uniquePatientOutpatientCount` is the patient-deduplicated metric preserved from model-layer data. Implement D-06 explicitly in the SQL semantic layer: expose `triggeredEvaluationCount` and `successEvaluatedCount` as separate metrics, and annotate in SQL comments or object naming that the homepage/default outward evaluated-case metric is `successEvaluatedCount`, not `triggeredEvaluationCount`. Implement D-07 explicitly in the SQL semantic layer: preserve both denominator variants for diagnosis-basis-incomplete and missing-diagnosis ratios, expose the success-evaluated denominator variants as the default outward metrics (`diagnosisBasisIncompleteRateBySuccess`, `missingDiagnosisRateBySuccess`), and retain the all-outpatient denominator variants (`diagnosisBasisIncompleteRateByEncounter`, `missingDiagnosisRateByEncounter`) in the semantic layer for internal or downstream reuse. Honor D-08 explicitly: keep the overall quality index only as an internal config-backed placeholder in Phase 1, for example via `analytics.metric_placeholder_config(metric_key text primary key, is_enabled boolean, placeholder_value numeric)` seeded with `metric_key = 'quality_index_score'`, `is_enabled = false`, `placeholder_value = null`; do not expose that placeholder through overview/trend SQL outputs or the outward TypeScript contract. Choose the preferred contract boundary: alias SQL outputs directly to camelCase so the SQL objects already return `OverviewSummaryRow` / `TrendSeriesRow` compatible field names and Plan 03 can consume them without ad hoc field remapping. Expose exact camelCase output fields including `outpatientCount`, `uniquePatientOutpatientCount`, `triggeredEvaluationCount`, `successEvaluatedCount`, `diagnosisBasisIncompleteRateBySuccess`, `diagnosisBasisIncompleteRateByEncounter`, `missingDiagnosisRateBySuccess`, `missingDiagnosisRateByEncounter`, `rangeStartDate`, `rangeEndDate`, `bucketGrain`, and for trend rows also `bucketStart`, `bucketEnd`, `bucketLabel`. Per D-15, overview and trend SQL objects must apply all supported filters in SQL itself: restrict to the resolved time window and add parameterized predicates for `departmentIds`, `departmentTypeIds`, `doctorIds`, and `diseaseIds`, using conditions such as `cardinality($n::text[]) = 0 or column = any($n::text[])` or the equivalent nullable-array pattern. Create `sql/tests/dash_01_default_window.sql` and `sql/tests/data_02_aggregates.sql` with assertions against the fixture dataset so the resolved dates, bucket grain, shift bucket generation rules, encounter-count, patient-deduplicated-count, separate triggered/success evaluation metrics, denominator-specific ratios, camelCase field aliases, absence of `qualityIndexScore` in outward rows, disabled internal placeholder config, and at least one department/doctor filter effect fail loudly if any value drifts. Each single assertion script should remain focused enough to serve as a sub-30-second feedback path on a local Phase 1 dataset.</action>
  <verify>
    <automated>bash sql/tests/run-phase-01.sh dash_01_default_window && bash sql/tests/run-phase-01.sh data_02_aggregates && rg -n "outpatientCount|uniquePatientOutpatientCount|departmentIds|departmentTypeIds|doctorIds|diseaseIds|metric_placeholder_config|quality_index_score|is_enabled = false|any\\(" sql/migrations/002_phase1_metrics.sql && ! rg -n "qualityIndexScore" src/domain/analytics/contracts.ts</automated>
  </verify>
  <done>默认时间口径和核心聚合逻辑已经固化在 SQL 层，概览与趋势可以从同一套时间窗口和分母规则出数。</done>
</task>

</tasks>

<verification>
Run `bash sql/tests/run-phase-01.sh dash_01_default_window` and `bash sql/tests/run-phase-01.sh data_02_aggregates` as separate fast checks rather than one broad suite. Each named assertion path is intended to remain under roughly 30 seconds on the local Phase 1 fixture dataset. Confirm the date-window assertions use explicit dates and the aggregate assertions validate encounter-count, unique-patient-count, both denominator variants, and the absence of `qualityIndexScore` from outward overview/trend outputs while the internal placeholder config remains disabled.
</verification>

<success_criteria>
`DATA-02` 与 `DASH-01` 对应的时间与聚合语义已可自动验证，且下游接口可直接复用 `contracts.ts` 与 SQL 输出字段。
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-data-foundation-02-SUMMARY.md`
</output>
