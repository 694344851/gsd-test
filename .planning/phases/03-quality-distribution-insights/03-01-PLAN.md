---
phase: 03-quality-distribution-insights
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sql/migrations/003_phase3_department_distribution.sql
  - sql/tests/run-phase-01.sh
  - sql/tests/dist_01_department_distribution.sql
  - src/domain/analytics/contracts.py
  - src/domain/analytics/query_service.py
  - src/api/analytics/department_distribution.py
  - src/api/http_dashboard_distribution.py
  - src/api/http_dashboard_server.py
  - frontend/src/app/dashboard-overview-page.tsx
  - frontend/src/styles/dashboard.css
  - frontend/src/lib/distribution-contracts.ts
  - frontend/src/lib/distribution-api.ts
  - frontend/src/lib/distribution-chart-options.ts
  - frontend/src/lib/__tests__/distribution-chart-options.test.ts
  - frontend/src/components/department-type-filter.tsx
  - frontend/src/components/department-distribution-chart.tsx
  - frontend/src/components/distribution-section.tsx
  - frontend/src/components/__tests__/distribution-section.test.tsx
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
must_haves:
  truths:
    - "首页新增科室诊断质量分布模块，按气泡图呈现门诊量、诊断依据不完整比例、评估病例数和缺失诊断比例。"
    - "科室类型筛选沿用页面共享 filters，不创建第二套时间/筛选状态。"
    - "用户可以直接操作科室类型筛选控件，且筛选变更会更新分布查询请求。"
    - "分布图 tooltip 能展示科室、科室类型和四个核心指标明细。"
    - "分布查询语义留在 SQL/Python/HTTP 边界，前端不推导聚合结果。"
  artifacts:
    - path: "sql/migrations/003_phase3_department_distribution.sql"
      provides: "Department distribution semantic query"
      contains: "analytics.get_department_distribution"
    - path: "src/api/http_dashboard_distribution.py"
      provides: "HTTP handler for /api/analytics/distribution"
      contains: "/api/analytics/distribution"
    - path: "frontend/src/components/distribution-section.tsx"
      provides: "Dashboard department distribution module"
      contains: "科室诊断质量分布"
    - path: "frontend/src/lib/distribution-chart-options.ts"
      provides: "Bubble/scatter chart option builder"
      contains: "symbolSize"
  key_links:
    - from: "src/domain/analytics/query_service.py"
      to: "sql/migrations/003_phase3_department_distribution.sql"
      via: "get_department_distribution delegates to analytics.get_department_distribution"
      pattern: "get_department_distribution"
    - from: "frontend/src/components/distribution-section.tsx"
      to: "frontend/src/lib/distribution-api.ts"
      via: "section loads distribution rows for active filters"
      pattern: "loadDepartmentDistribution"
    - from: "frontend/src/lib/distribution-chart-options.ts"
      to: "frontend/src/lib/distribution-contracts.ts"
      via: "chart maps department rows into bubble series and tooltip rows"
      pattern: "outpatient_count|department_name|missing_diagnosis_rate_by_success"
---

<objective>
交付 Phase 3 的科室诊断质量分布模块，包括后端分布查询、科室类型筛选接入与首页气泡图。

Purpose: 先把 Phase 3 的第一个新洞察模块打通，验证 Phase 2 的共享筛选壳可以继续承载新增可视化，不必重做 dashboard 架构。
Output: Department distribution SQL/Python/API contract、ECharts 气泡图、tooltip 与模块级空态/异常态测试。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-quality-distribution-insights/03-RESEARCH.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-VERIFICATION.md
@.planning/phases/02-dashboard-overview/02-CONTEXT.md
@.planning/phases/02-dashboard-overview/02-UI-SPEC.md
@.planning/phases/02-dashboard-overview/02-dashboard-overview-01-SUMMARY.md
@.planning/phases/02-dashboard-overview/02-dashboard-overview-02-SUMMARY.md
@.planning/phases/02-dashboard-overview/02-dashboard-overview-03-SUMMARY.md
@门诊诊鉴首页-(全栈).md

<interfaces>
Plan 03-01 must add a backend response shaped like:
```ts
export interface DepartmentDistributionResponse {
  filters: {
    as_of_date: string;
    range_key: string;
    start_date: string | null;
    end_date: string | null;
    department_type_ids: string[] | null;
  };
  available_department_types: Array<{
    department_type_id: string;
    department_type_name: string;
  }>;
  rows: Array<{
    department_id: string;
    department_name: string;
    department_type_id: string;
    department_type_name: string;
    outpatient_count: number;
    success_evaluated_count: number;
    diagnosis_basis_incomplete_rate_by_success: number | null;
    missing_diagnosis_rate_by_success: number | null;
  }>;
}
```

Frontend visualization contract:
```ts
export interface DistributionSectionProps {
  filters: DashboardFilters;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add department distribution semantic query, API contract, and SQL assertions</name>
  <read_first>
    .planning/phases/03-quality-distribution-insights/03-RESEARCH.md
    .planning/phases/01-data-foundation/01-CONTEXT.md
    .planning/phases/01-data-foundation/01-VERIFICATION.md
    sql/migrations/001_phase1_foundation.sql
    sql/migrations/002_phase1_metrics.sql
    src/domain/analytics/contracts.py
    src/domain/analytics/query_service.py
    src/api/http_dashboard_server.py
  </read_first>
  <files>sql/migrations/003_phase3_department_distribution.sql, sql/tests/run-phase-01.sh, sql/tests/dist_01_department_distribution.sql, src/domain/analytics/contracts.py, src/domain/analytics/query_service.py, src/api/analytics/department_distribution.py, src/api/http_dashboard_distribution.py, src/api/http_dashboard_server.py, frontend/src/lib/distribution-contracts.ts, frontend/src/lib/distribution-api.ts</files>
  <acceptance_criteria>
    - `sql/migrations/003_phase3_department_distribution.sql` defines `analytics.get_department_distribution`.
    - The SQL output contains `department_name`, `department_type_name`, `outpatient_count`, `success_evaluated_count`, `diagnosis_basis_incomplete_rate_by_success`, and `missing_diagnosis_rate_by_success`.
    - The HTTP/API contract includes `available_department_types` so the frontend can render a real selector instead of hard-coded options.
    - `src/domain/analytics/query_service.py` exports `get_department_distribution`.
    - `src/api/http_dashboard_distribution.py` exposes `handle_distribution`.
    - `src/api/http_dashboard_server.py` mounts `"/api/analytics/distribution"`.
    - `sql/tests/dist_01_department_distribution.sql` asserts the `department_type_ids` filter changes the returned rows.
    - `sql/tests/run-phase-01.sh` accepts the test name `dist_01_department_distribution`.
    - `frontend/src/lib/distribution-api.ts` fetches `'/api/analytics/distribution'` using `range_key`, `as_of_date`, and `department_type_ids`.
  </acceptance_criteria>
  <action>Implement the department distribution backend path without disturbing Phase 1/2 semantics. In `sql/migrations/003_phase3_department_distribution.sql`, add `analytics.get_department_distribution(range_key, as_of_date, custom_start, custom_end, departmentIds, departmentTypeIds, doctorIds, diseaseIds)` that groups from `analytics.fact_outpatient_encounter` joined to `analytics.mart_case_evaluation`, producing one row per department in the selected time window. X-axis metric is `outpatient_count`; bubble size metric is `success_evaluated_count`; Y-axis metric is `diagnosis_basis_incomplete_rate_by_success`; bubble color metric is `missing_diagnosis_rate_by_success`. Preserve nullable ratios when there are zero successful evaluations rather than forcing zeroes. Include department and department type display fields by joining the dimension tables from Phase 1, and expose `available_department_types` in the API payload from the same filtered scope so the frontend can render a user-operable selector without hard-coding options. Extend `src/domain/analytics/contracts.py` and `src/domain/analytics/query_service.py` with a Python dataclass and query function for this result. Add `src/api/analytics/department_distribution.py` and `src/api/http_dashboard_distribution.py`, then mount `GET /api/analytics/distribution` in `src/api/http_dashboard_server.py`, following the existing overview/trend handler pattern exactly. Extend `sql/tests/run-phase-01.sh` to accept `dist_01_department_distribution`, and create `sql/tests/dist_01_department_distribution.sql` with focused assertions proving the query returns department-level rows for `as_of_date = DATE '2026-03-24'`, that the `departmentTypeIds` filter reduces or changes the result set, and that at least one available department type option is present. Add matching frontend contracts in `frontend/src/lib/distribution-contracts.ts` and `frontend/src/lib/distribution-api.ts`, preserving backend snake_case fields and exact filter query keys. Do not implement the frontend chart in this task; stop at the stable query/API boundary.</action>
  <verify>
    <automated>export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh dist_01_department_distribution && python3 -m py_compile src/domain/analytics/contracts.py src/domain/analytics/query_service.py src/api/analytics/department_distribution.py src/api/http_dashboard_distribution.py src/api/http_dashboard_server.py && rg -n "get_department_distribution|/api/analytics/distribution|department_type_ids|missing_diagnosis_rate_by_success" sql/migrations/003_phase3_department_distribution.sql sql/tests/dist_01_department_distribution.sql src/domain/analytics/query_service.py src/api/http_dashboard_distribution.py src/api/http_dashboard_server.py frontend/src/lib/distribution-api.ts frontend/src/lib/distribution-contracts.ts</automated>
  </verify>
  <done>科室分布模块已经具备稳定的 SQL/Python/API 数据边界，前端只需消费结果渲染气泡图。</done>
</task>

<task type="auto">
  <name>Task 2: Render the department bubble chart with shared filters, tooltip, and module states</name>
  <read_first>
    .planning/phases/03-quality-distribution-insights/03-RESEARCH.md
    .planning/phases/02-dashboard-overview/02-UI-SPEC.md
    frontend/src/app/dashboard-overview-page.tsx
    frontend/src/styles/dashboard.css
    frontend/src/components/trend-section.tsx
    frontend/src/lib/chart-options.ts
    frontend/src/lib/distribution-contracts.ts
    frontend/src/lib/distribution-api.ts
  </read_first>
  <files>frontend/src/app/dashboard-overview-page.tsx, frontend/src/styles/dashboard.css, frontend/src/lib/distribution-chart-options.ts, frontend/src/lib/__tests__/distribution-chart-options.test.ts, frontend/src/components/department-type-filter.tsx, frontend/src/components/department-distribution-chart.tsx, frontend/src/components/distribution-section.tsx, frontend/src/components/__tests__/distribution-section.test.tsx</files>
  <acceptance_criteria>
    - `frontend/src/components/distribution-section.tsx` contains the heading `科室诊断质量分布`.
    - `frontend/src/components/department-type-filter.tsx` renders a user-operable selector labeled `科室类型`.
    - `frontend/src/lib/distribution-chart-options.ts` uses a scatter-style series with `symbolSize`.
    - `frontend/src/lib/distribution-chart-options.ts` reads `department_name` and does not derive department labels in the browser.
    - The tooltip includes `门诊量`, `评估病例数`, `诊断依据不完整比例`, and `缺失诊断比例`.
    - `frontend/src/components/distribution-section.tsx` fetches through `loadDepartmentDistribution(`.
    - `frontend/src/components/distribution-section.tsx` supports empty and error states instead of showing an empty canvas.
    - Changing the department-type selector updates the shared page filters and changes the distribution request.
    - `frontend/src/app/dashboard-overview-page.tsx` renders the distribution section after the existing trend section.
    - `frontend/src/components/__tests__/distribution-section.test.tsx` verifies successful render, empty state, error state, and department-type filter interaction.
  </acceptance_criteria>
  <action>Implement the Phase 3 department distribution module in the existing dashboard shell. In `frontend/src/lib/distribution-chart-options.ts`, create a pure ECharts option builder for a bubble/scatter chart that maps each department row into a point where x=`outpatient_count`, y=`diagnosis_basis_incomplete_rate_by_success * 100`, size=`success_evaluated_count`, and color encodes `missing_diagnosis_rate_by_success`. Tooltip rows must show department name, department type, 门诊量, 评估病例数, 诊断依据不完整比例, and 缺失诊断比例. Keep null ratios visible as `--` in tooltip and do not coerce them to zero. Add `frontend/src/components/department-distribution-chart.tsx` as a thin `ReactECharts` wrapper. Add `frontend/src/components/department-type-filter.tsx` as a real selector control labeled `科室类型`, sourcing options from `available_department_types` returned by the distribution API. In `frontend/src/components/distribution-section.tsx`, call `loadDepartmentDistribution(filters)`, render the selector and chart with a section heading `科室诊断质量分布`, and show module-level loading, empty, and error states consistent with the existing dashboard style. Wire the selector into shared page-level `DashboardFilters` via `DashboardOverviewPage`, so changing department type updates the shared filter state and the subsequent distribution request. Update `frontend/src/app/dashboard-overview-page.tsx` and `frontend/src/styles/dashboard.css` so the new section sits below the existing trend chart and inherits the Phase 2 token system. Do not add a second time filter or local-only filter source.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/__tests__/distribution-chart-options.test.ts src/components/__tests__/distribution-section.test.tsx src/app/__tests__/dashboard-overview-page.test.tsx && npm run build</automated>
  </verify>
  <done>首页已经能基于共享筛选展示科室诊断质量分布气泡图，满足 DIST-01/02/03 的可视化与交互边界。</done>
</task>

</tasks>

<verification>
执行 `export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh dist_01_department_distribution`，再执行 `cd frontend && npm test -- --run src/lib/__tests__/distribution-chart-options.test.ts src/components/__tests__/distribution-section.test.tsx && npm run build`。
</verification>

<success_criteria>
`DIST-01`、`DIST-02`、`DIST-03` 对应的后端分布查询、科室类型筛选和首页气泡图模块均已具备可验证实现。
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-distribution-insights/03-quality-distribution-insights-01-SUMMARY.md`
</output>
