---
phase: 03-quality-distribution-insights
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - sql/migrations/004_phase3_disease_insights.sql
  - sql/tests/run-phase-01.sh
  - sql/tests/dise_01_disease_insights.sql
  - src/domain/analytics/contracts.py
  - src/domain/analytics/query_service.py
  - src/api/analytics/disease_insights.py
  - src/api/http_dashboard_disease.py
  - src/api/http_dashboard_server.py
  - frontend/src/app/dashboard-overview-page.tsx
  - frontend/src/styles/dashboard.css
  - frontend/src/lib/disease-insights-contracts.ts
  - frontend/src/lib/disease-insights-api.ts
  - frontend/src/lib/disease-cloud-layout.ts
  - frontend/src/lib/__tests__/disease-cloud-layout.test.ts
  - frontend/src/components/disease-insights-section.tsx
  - frontend/src/components/__tests__/disease-insights-section.test.tsx
  - frontend/src/app/__tests__/dashboard-overview-page.test.tsx
autonomous: true
requirements:
  - DISE-01
  - DISE-02
must_haves:
  truths:
    - "首页新增高发问题病种模块，默认展示问题数量靠前的病种。"
    - "病种严重程度颜色与排序逻辑稳定可验证，而不是随机视觉效果。"
    - "词云布局按严重程度从中心向外分层，而不是仅按列表顺序渲染。"
    - "病种洞察查询结果由后端提供，前端不自行重算问题排名。"
  artifacts:
    - path: "sql/migrations/004_phase3_disease_insights.sql"
      provides: "Disease ranking semantic query"
      contains: "analytics.get_disease_insights"
    - path: "frontend/src/lib/disease-cloud-layout.ts"
      provides: "Deterministic word-cloud-like layout helper"
      contains: "center_ring"
    - path: "frontend/src/components/disease-insights-section.tsx"
      provides: "Dashboard disease insight module"
      contains: "高发问题病种"
  key_links:
    - from: "src/domain/analytics/query_service.py"
      to: "sql/migrations/004_phase3_disease_insights.sql"
      via: "get_disease_insights delegates to analytics.get_disease_insights"
      pattern: "get_disease_insights"
    - from: "frontend/src/components/disease-insights-section.tsx"
      to: "frontend/src/lib/disease-insights-api.ts"
      via: "section loads top disease rows for active filters"
      pattern: "loadDiseaseInsights"
    - from: "frontend/src/components/disease-insights-section.tsx"
      to: "frontend/src/lib/disease-cloud-layout.ts"
      via: "section renders deterministic cloud items from ranked rows"
      pattern: "buildDiseaseCloudLayout|center_ring"
---

<objective>
交付 Phase 3 的高发问题病种词云模块，包括病种排序语义、严重程度颜色规则和首页展示。

Purpose: 在科室分布之后补齐首页第二个洞察模块，完成 Phase 3 对“分布 + 问题病种”的整体交付。
Output: Disease ranking SQL/Python/API contract、deterministic word-cloud-style layout、模块状态测试。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-quality-distribution-insights/03-RESEARCH.md
@.planning/phases/03-quality-distribution-insights/03-01-PLAN.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-VERIFICATION.md
@.planning/phases/02-dashboard-overview/02-CONTEXT.md
@.planning/phases/02-dashboard-overview/02-UI-SPEC.md
@门诊诊鉴首页-(全栈).md

<interfaces>
Plan 03-02 must add a backend response shaped like:
```ts
export interface DiseaseInsightsResponse {
  filters: {
    as_of_date: string;
    range_key: string;
    start_date: string | null;
    end_date: string | null;
  };
  rows: Array<{
    disease_id: string;
    disease_name: string;
    issue_count: number;
    diagnosis_basis_incomplete_count: number;
    missing_diagnosis_count: number;
    severity_ratio: number;
    severity_band: 'top_20' | 'top_50' | 'top_70' | 'tail';
  }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add disease-insights semantic query, ranking rules, and API contract</name>
  <read_first>
    .planning/phases/03-quality-distribution-insights/03-RESEARCH.md
    .planning/phases/01-data-foundation/01-CONTEXT.md
    .planning/phases/01-data-foundation/01-VERIFICATION.md
    门诊诊鉴首页-(全栈).md
    src/domain/analytics/contracts.py
    src/domain/analytics/query_service.py
    src/api/http_dashboard_server.py
  </read_first>
  <files>sql/migrations/004_phase3_disease_insights.sql, sql/tests/run-phase-01.sh, sql/tests/dise_01_disease_insights.sql, src/domain/analytics/contracts.py, src/domain/analytics/query_service.py, src/api/analytics/disease_insights.py, src/api/http_dashboard_disease.py, src/api/http_dashboard_server.py, frontend/src/lib/disease-insights-contracts.ts, frontend/src/lib/disease-insights-api.ts</files>
  <acceptance_criteria>
    - `sql/migrations/004_phase3_disease_insights.sql` defines `analytics.get_disease_insights`.
    - The SQL output contains `disease_name`, `issue_count`, `severity_ratio`, and `severity_band`.
    - `sql/tests/dise_01_disease_insights.sql` asserts top diseases are ordered by issue count and severity logic.
    - `src/domain/analytics/query_service.py` exports `get_disease_insights`.
    - `src/api/http_dashboard_disease.py` exposes `handle_disease_insights`.
    - `src/api/http_dashboard_server.py` mounts `"/api/analytics/disease-insights"`.
    - `frontend/src/lib/disease-insights-api.ts` fetches `'/api/analytics/disease-insights'`.
  </acceptance_criteria>
  <action>Implement the disease ranking backend path. In `sql/migrations/004_phase3_disease_insights.sql`, add `analytics.get_disease_insights(range_key, as_of_date, custom_start, custom_end, departmentIds, departmentTypeIds, doctorIds, diseaseIds)` that ranks diseases by combined issue count (`diagnosis_basis_incomplete` + `missing_diagnosis`) for the current filtered window. Return at most the top 20 diseases by default, include separate incomplete/missing counts, a normalized `severity_ratio`, and a stable `severity_band` bucket matching the PRD intent: top 20%, 20%-50%, 50%-70%, tail, using left-closed/right-open assignment. Preserve filtered scope and do not compute ranking in the browser. Extend the Python contracts/query service and add `src/api/analytics/disease_insights.py` plus `src/api/http_dashboard_disease.py`, then mount `GET /api/analytics/disease-insights` in the HTTP server. Extend the SQL test runner to accept `dise_01_disease_insights` and add assertions that the result set is ranked, bounded, and stable for the seeded fixture dataset. Add matching frontend contracts and API loader files preserving backend snake_case fields.</action>
  <verify>
    <automated>export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh dise_01_disease_insights && python3 -m py_compile src/domain/analytics/contracts.py src/domain/analytics/query_service.py src/api/analytics/disease_insights.py src/api/http_dashboard_disease.py src/api/http_dashboard_server.py && rg -n "get_disease_insights|/api/analytics/disease-insights|severity_band|issue_count" sql/migrations/004_phase3_disease_insights.sql sql/tests/dise_01_disease_insights.sql src/domain/analytics/query_service.py src/api/http_dashboard_disease.py src/api/http_dashboard_server.py frontend/src/lib/disease-insights-api.ts frontend/src/lib/disease-insights-contracts.ts</automated>
  </verify>
  <done>病种洞察模块已经具备稳定的排名和严重程度后端语义，前端只需要布局与呈现。</done>
</task>

<task type="auto">
  <name>Task 2: Implement the disease insight section with deterministic word-cloud layout</name>
  <read_first>
    .planning/phases/03-quality-distribution-insights/03-RESEARCH.md
    .planning/phases/02-dashboard-overview/02-UI-SPEC.md
    frontend/src/app/dashboard-overview-page.tsx
    frontend/src/styles/dashboard.css
    frontend/src/lib/disease-insights-contracts.ts
    frontend/src/lib/disease-insights-api.ts
  </read_first>
  <files>frontend/src/app/dashboard-overview-page.tsx, frontend/src/styles/dashboard.css, frontend/src/lib/disease-cloud-layout.ts, frontend/src/lib/__tests__/disease-cloud-layout.test.ts, frontend/src/components/disease-insights-section.tsx, frontend/src/components/__tests__/disease-insights-section.test.tsx</files>
  <acceptance_criteria>
    - `frontend/src/components/disease-insights-section.tsx` contains the heading `高发问题病种`.
    - `frontend/src/lib/disease-cloud-layout.ts` contains `top_20`, `top_50`, `top_70`, and `tail`.
    - `frontend/src/lib/disease-cloud-layout.ts` explicitly maps severity bands to center-outward layout groups such as `center_ring`, `inner_ring`, `mid_ring`, and `outer_ring`.
    - The section fetches through `loadDiseaseInsights(`.
    - The rendered module does not randomize order on each render.
    - More severe items render closer to the center and use darker color tiers than less severe items.
    - The section supports empty and error states instead of showing a blank block.
    - `frontend/src/app/dashboard-overview-page.tsx` renders the disease section after the distribution section.
    - `frontend/src/components/__tests__/disease-insights-section.test.tsx` verifies successful render, empty state, and error state.
    - `frontend/src/lib/__tests__/disease-cloud-layout.test.ts` verifies left-closed/right-open band assignment, center-outward position grouping, and dark-to-light severity color mapping.
  </acceptance_criteria>
  <action>Implement a deterministic word-cloud-style disease insight module without adding a new visualization dependency. In `frontend/src/lib/disease-cloud-layout.ts`, build a pure helper that takes the ranked disease rows and returns positioned display items with stable font size, color class, and explicit center-outward layout groups derived from `severity_band`. Encode the PRD layout contract directly: `top_20` items occupy the center ring, `top_50` the next ring, `top_70` the next ring, and `tail` the outer ring, with left-closed/right-open severity band assignment preserved from the backend contract. Keep the color semantics aligned with the PRD’s severity tiers and Phase 2’s token palette extensions so more severe items are darker and visually more central. In `frontend/src/components/disease-insights-section.tsx`, load rows from `loadDiseaseInsights(filters)`, render a module heading `高发问题病种`, and display the top diseases in this stable cloud-style layout while preserving backend rank order inside each ring. Add module-level loading, empty, and error states consistent with the rest of the dashboard. Update `frontend/src/app/dashboard-overview-page.tsx` and `frontend/src/styles/dashboard.css` so the disease module appears after the distribution module and uses the existing dashboard surface/layout system. Do not introduce a third-party word cloud library, and do not randomize placement on each render.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/__tests__/disease-cloud-layout.test.ts src/components/__tests__/disease-insights-section.test.tsx src/app/__tests__/dashboard-overview-page.test.tsx && npm run build</automated>
  </verify>
  <done>首页已经具备高发问题病种词云式模块，满足 DISE-01/02 对排序、严重程度表达和模块状态的要求。</done>
</task>

</tasks>

<verification>
执行 `export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh dise_01_disease_insights`，再执行 `cd frontend && npm test -- --run src/lib/__tests__/disease-cloud-layout.test.ts src/components/__tests__/disease-insights-section.test.tsx && npm run build`。
</verification>

<success_criteria>
`DISE-01` 与 `DISE-02` 对应的病种排名语义、严重程度颜色规则和首页高发问题病种模块都已具备自动验证。
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-distribution-insights/03-quality-distribution-insights-02-SUMMARY.md`
</output>
