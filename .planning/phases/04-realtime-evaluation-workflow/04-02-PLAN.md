---
phase: 04-realtime-evaluation-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/api/http_dashboard_server.py
  - src/api/http_realtime_evaluation.py
  - src/api/evaluation/contracts.py
  - src/api/evaluation/orchestration.py
  - src/api/evaluation/provider.py
  - src/api/evaluation/smoke.py
  - frontend/src/lib/realtime-evaluation-api.ts
  - frontend/src/components/doctor-evaluation-panel.tsx
  - frontend/src/components/__tests__/doctor-evaluation-panel.test.tsx
autonomous: true
requirements:
  - EVAL-02
  - EVAL-03
must_haves:
  truths:
    - "实时诊鉴通过新的 POST 接口执行，而不是在前端拼接假结果。"
    - "服务编排必须定义明确的 timeout、failed、success 三类结果语义。"
    - "10 秒目标需要体现在后端超时控制和前端 timeout 状态上，而不是只写在文档里。"
    - "接口在 success、timeout、failed 三种结果下都返回统一结构，不向医生暴露底层 provider 细节。"
  artifacts:
    - path: "src/api/http_realtime_evaluation.py"
      provides: "HTTP POST handler for realtime evaluation"
      contains: "handle_realtime_evaluation"
    - path: "src/api/evaluation/orchestration.py"
      provides: "Timeout-controlled evaluation orchestration"
      contains: "timeout_seconds"
    - path: "src/api/evaluation/provider.py"
      provides: "Swappable evaluator provider interface"
      contains: "evaluate"
    - path: "src/api/evaluation/smoke.py"
      provides: "Deterministic smoke coverage for success/timeout/failed execution paths"
      contains: "main"
  key_links:
    - from: "src/api/http_realtime_evaluation.py"
      to: "src/api/evaluation/orchestration.py"
      via: "handler delegates execution to orchestration rather than embedding provider logic"
      pattern: "run_realtime_evaluation"
    - from: "src/api/evaluation/orchestration.py"
      to: "src/api/evaluation/provider.py"
      via: "orchestration calls an evaluator adapter under timeout control"
      pattern: "evaluate"
    - from: "frontend/src/lib/realtime-evaluation-api.ts"
      to: "src/api/http_realtime_evaluation.py"
      via: "frontend POSTs typed JSON to the realtime evaluation endpoint"
      pattern: "/api/realtime-evaluation"
---

<objective>
交付实时诊鉴的后端执行编排，包括 POST 接口、超时控制、provider adapter 和结构化结果返回。

Purpose: 把 Phase 4 从“会显示结果的前端壳”推进到“可真正执行且具备 timeout/failed 语义的医生端工作流”。
Output: realtime evaluation HTTP endpoint、typed Python contracts、orchestration service、provider adapter、handler/orchestration smoke、前后端 timeout/error 集成测试。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-realtime-evaluation-workflow/04-RESEARCH.md
@.planning/phases/04-realtime-evaluation-workflow/04-01-PLAN.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@src/api/http_dashboard_server.py
@src/domain/analytics/query_service.py
@门诊诊鉴首页-(全栈).md

<interfaces>
Plan 04-02 must expose an HTTP boundary shaped like:
```http
POST /api/realtime-evaluation
Content-Type: application/json
```

And a backend contract shaped like:
```py
@dataclass(slots=True)
class RealtimeEvaluationResult:
    evaluation_id: str
    status: Literal["success", "timeout", "failed"]
    elapsed_ms: int
    assistive_notice: str
    basis_completeness: dict[str, Any]
    potential_missing_diagnoses: list[dict[str, Any]]
    rationale: list[str]
    suggestions: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add realtime evaluation POST handler and typed orchestration boundary</name>
  <read_first>
    .planning/phases/04-realtime-evaluation-workflow/04-RESEARCH.md
    .planning/phases/04-realtime-evaluation-workflow/04-01-PLAN.md
    src/api/http_dashboard_server.py
    src/api/http_dashboard_overview.py
    frontend/src/lib/realtime-evaluation-contracts.ts
  </read_first>
  <files>src/api/http_dashboard_server.py, src/api/http_realtime_evaluation.py, src/api/evaluation/contracts.py, src/api/evaluation/orchestration.py, src/api/evaluation/provider.py, src/api/evaluation/smoke.py</files>
  <acceptance_criteria>
    - `src/api/http_realtime_evaluation.py` parses JSON request bodies and exposes `handle_realtime_evaluation`.
    - `src/api/http_dashboard_server.py` dispatches POST realtime evaluation requests.
    - `src/api/evaluation/contracts.py` defines typed request/result contracts.
    - `src/api/evaluation/provider.py` defines an adapter interface instead of hard-coding provider logic in the handler.
    - `src/api/evaluation/orchestration.py` implements an explicit timeout budget near 10 seconds.
    - Timeout and failure responses return normalized `status` fields rather than uncaught exceptions.
    - `python3 src/api/evaluation/smoke.py` exits 0 after exercising success, timeout, and failed branches against the handler/orchestration boundary.
  </acceptance_criteria>
  <action>Implement the backend runtime path for doctor-side realtime evaluation. Add `src/api/http_realtime_evaluation.py` to parse `POST /api/realtime-evaluation` JSON payloads, validate required request fields, and delegate execution. Add or extend `src/api/evaluation/contracts.py`, `src/api/evaluation/provider.py`, and `src/api/evaluation/orchestration.py` so the backend exposes a typed request/result boundary and an explicit timeout budget around the PRD’s 10-second expectation. Update `src/api/http_dashboard_server.py` to dispatch realtime POST requests alongside the existing GET analytics routes. The orchestration layer must normalize three outcome classes: `success`, `timeout`, and `failed`, each mapped back into the same structured response shape expected by the frontend. Add `src/api/evaluation/smoke.py` as a deterministic direct-run smoke script that uses fake provider behaviors to prove the handler/orchestration path returns the normalized success, timeout, and failed outcomes without needing `pytest`. Do not persist results yet in this task; durable repository work belongs to 04-03.</action>
  <verify>
    <automated>python3 -m py_compile src/api/http_dashboard_server.py src/api/http_realtime_evaluation.py src/api/evaluation/contracts.py src/api/evaluation/orchestration.py src/api/evaluation/provider.py src/api/evaluation/smoke.py && python3 src/api/evaluation/smoke.py</automated>
  </verify>
  <done>后端已经具备受控的 realtime execution 边界，前端可以不再依赖纯 mock 结果。</done>
</task>

<task type="auto">
  <name>Task 2: Wire frontend API calls to the new endpoint and verify timeout/error behavior</name>
  <read_first>
    .planning/phases/04-realtime-evaluation-workflow/04-RESEARCH.md
    frontend/src/lib/realtime-evaluation-api.ts
    frontend/src/components/doctor-evaluation-panel.tsx
    src/api/http_realtime_evaluation.py
  </read_first>
  <files>frontend/src/lib/realtime-evaluation-api.ts, frontend/src/components/doctor-evaluation-panel.tsx, frontend/src/components/__tests__/doctor-evaluation-panel.test.tsx</files>
  <acceptance_criteria>
    - `frontend/src/lib/realtime-evaluation-api.ts` POSTs JSON to `'/api/realtime-evaluation'`.
    - `frontend/src/components/doctor-evaluation-panel.tsx` surfaces timeout and failed states based on backend `status`.
    - `frontend/src/components/__tests__/doctor-evaluation-panel.test.tsx` verifies success, timeout, and failed response handling.
    - `frontend/src/components/doctor-evaluation-panel.tsx` preserves an injectable client/callback seam so tests stay deterministic while production uses the real POST wrapper.
  </acceptance_criteria>
  <action>Connect the frontend shell from 04-01 to the new backend endpoint. Add `frontend/src/lib/realtime-evaluation-api.ts` with a typed `runRealtimeEvaluation(request)` wrapper that submits JSON to `POST /api/realtime-evaluation` and preserves the structured backend payload. Update `frontend/src/components/doctor-evaluation-panel.tsx` so its default path uses `runRealtimeEvaluation`, but retain an injectable client/callback seam so the tests from 04-01 remain deterministic. Drive UI state from backend `status` instead of local guesses, and explicitly render timeout and failed branches using the same assistive tone. Add frontend tests that mock distinct backend payloads and prove success, timeout, and failed behaviors are visible and deterministic.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/__tests__/doctor-evaluation-panel.test.tsx src/app/__tests__/doctor-evaluation-page.test.tsx</automated>
  </verify>
  <done>医生端已经能通过真实 POST 接口触发诊鉴并稳定呈现 success/timeout/failed 结果，满足 EVAL-02 的执行边界。</done>
</task>

</tasks>

<verification>
执行 `python3 -m py_compile src/api/http_dashboard_server.py src/api/http_realtime_evaluation.py src/api/evaluation/contracts.py src/api/evaluation/orchestration.py src/api/evaluation/provider.py src/api/evaluation/smoke.py`，再执行 `python3 src/api/evaluation/smoke.py`，最后执行 `cd frontend && npm test -- --run src/components/__tests__/doctor-evaluation-panel.test.tsx src/app/__tests__/doctor-evaluation-page.test.tsx`。
</verification>

<success_criteria>
`EVAL-02` 与 `EVAL-03` 对应的执行编排、timeout 语义和结构化结果返回边界已经明确且可验证。
</success_criteria>

<output>
After completion, create `.planning/phases/04-realtime-evaluation-workflow/04-realtime-evaluation-workflow-02-SUMMARY.md`
</output>
