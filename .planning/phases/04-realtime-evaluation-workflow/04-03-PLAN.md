---
phase: 04-realtime-evaluation-workflow
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - sql/migrations/005_phase4_realtime_evaluation.sql
  - sql/tests/run-phase-01.sh
  - sql/tests/eval_01_persistence_chain.sql
  - src/api/evaluation/repository.py
  - src/api/evaluation/orchestration.py
  - src/domain/analytics/contracts.py
  - src/domain/analytics/query_service.py
  - src/domain/analytics/reuse_smoke.py
  - src/api/http_realtime_evaluation.py
  - frontend/src/components/doctor-evaluation-panel.tsx
autonomous: true
requirements:
  - EVAL-04
  - EVAL-02
must_haves:
  truths:
    - "每次诊鉴请求无论 success、timeout、failed 都必须留下可追踪记录。"
    - "持久化链路必须与现有 Phase 1 analytics 语义兼容，而不是另起一套统计模型。"
    - "结构化详情可以单独存储，但 aggregate-friendly summary flags 必须可被统计层复用。"
    - "不能在医生端 10 秒响应链路里引入重型 dashboard 全量刷新。"
  artifacts:
    - path: "sql/migrations/005_phase4_realtime_evaluation.sql"
      provides: "Realtime evaluation persistence / reuse schema"
      contains: "realtime_evaluation"
    - path: "src/api/evaluation/repository.py"
      provides: "Durable write path for evaluation attempts and results"
      contains: "create_attempt"
    - path: "sql/tests/eval_01_persistence_chain.sql"
      provides: "SQL assertion for persisted evaluation reuse"
      contains: "success|timeout|failed"
  key_links:
    - from: "src/api/evaluation/orchestration.py"
      to: "src/api/evaluation/repository.py"
      via: "orchestration persists attempt start and final outcome"
      pattern: "create_attempt|mark_success|mark_timeout|mark_failed"
    - from: "src/api/evaluation/repository.py"
      to: "sql/migrations/005_phase4_realtime_evaluation.sql"
      via: "repository writes into the Phase 4 persistence schema"
      pattern: "insert into"
    - from: "src/domain/analytics/query_service.py"
      to: "sql/migrations/005_phase4_realtime_evaluation.sql"
      via: "analytics reuse reads persisted realtime evaluation summaries without a parallel model"
      pattern: "realtime_evaluation|evaluation_detail"
---

<objective>
交付实时诊鉴的持久化与 analytics reuse 链路，确保每次医生触发都能被存档并供统计/下钻继续复用。

Purpose: 完成 EVAL-04，把 Phase 4 从“能实时返回结果”推进到“结果可追踪、可复用、可进入后续分析”的完整闭环。
Output: SQL persistence schema、repository write path、outcome state persistence、analytics reuse tests。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-realtime-evaluation-workflow/04-RESEARCH.md
@.planning/phases/04-realtime-evaluation-workflow/04-02-PLAN.md
@.planning/phases/01-data-foundation/01-VERIFICATION.md
@sql/migrations/001_phase1_foundation.sql
@src/domain/analytics/query_service.py
@门诊诊鉴首页-(全栈).md

<interfaces>
Plan 04-03 must add a persistence path that can represent:
```sql
status in ('pending', 'success', 'timeout', 'failed')
```

And preserve analytics-friendly summary fields such as:
```ts
{
  diagnosis_basis_incomplete: boolean;
  missing_diagnosis: boolean;
  quality_index_score: number | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 4 persistence schema and repository write path</name>
  <read_first>
    .planning/phases/04-realtime-evaluation-workflow/04-RESEARCH.md
    .planning/phases/04-realtime-evaluation-workflow/04-02-PLAN.md
    sql/migrations/001_phase1_foundation.sql
    src/api/evaluation/orchestration.py
    src/api/evaluation/repository.py
  </read_first>
  <files>sql/migrations/005_phase4_realtime_evaluation.sql, sql/tests/run-phase-01.sh, sql/tests/eval_01_persistence_chain.sql, src/api/evaluation/repository.py, src/api/evaluation/orchestration.py, src/api/http_realtime_evaluation.py</files>
  <acceptance_criteria>
    - `sql/migrations/005_phase4_realtime_evaluation.sql` defines durable Phase 4 tables or bridge objects for realtime evaluation attempts/results.
    - `src/api/evaluation/repository.py` exposes `create_attempt`, `mark_success`, `mark_timeout`, and `mark_failed`.
    - `src/api/evaluation/orchestration.py` persists both attempt start and final outcome.
    - `sql/tests/eval_01_persistence_chain.sql` proves success, timeout, and failed states can all be persisted.
    - `sql/tests/run-phase-01.sh` accepts the new Phase 4 SQL test name.
  </acceptance_criteria>
  <action>Implement durable persistence for realtime evaluations. Add `sql/migrations/005_phase4_realtime_evaluation.sql` with the minimal schema needed to persist each evaluation attempt plus any structured detail payload required for later drilldown. Extend `src/api/evaluation/repository.py` from a boundary into a real write path exposing `create_attempt`, `mark_success`, `mark_timeout`, and `mark_failed`, and wire `src/api/evaluation/orchestration.py` to call these methods around provider execution. Update `src/api/http_realtime_evaluation.py` as needed so response payloads include persisted `evaluation_id` values. Add a focused SQL assertion file `sql/tests/eval_01_persistence_chain.sql` and extend `sql/tests/run-phase-01.sh` so the test can be executed through the existing repo test entrypoint. Keep provider detail payloads structured, but also persist aggregate-friendly booleans needed for later statistics reuse.</action>
  <verify>
    <automated>export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh eval_01_persistence_chain && python3 -m py_compile src/api/evaluation/repository.py src/api/evaluation/orchestration.py src/api/http_realtime_evaluation.py</automated>
  </verify>
  <done>每次诊鉴已经具备持久化记录与 outcome trace，医生侧响应不再是一次性瞬时结果。</done>
</task>

<task type="auto">
  <name>Task 2: Bridge persisted evaluation outcomes into analytics reuse</name>
  <read_first>
    .planning/phases/04-realtime-evaluation-workflow/04-RESEARCH.md
    sql/migrations/005_phase4_realtime_evaluation.sql
    src/domain/analytics/contracts.py
    src/domain/analytics/query_service.py
    .planning/phases/01-data-foundation/01-VERIFICATION.md
  </read_first>
  <files>src/domain/analytics/contracts.py, src/domain/analytics/query_service.py, src/domain/analytics/reuse_smoke.py, src/api/evaluation/repository.py, frontend/src/components/doctor-evaluation-panel.tsx</files>
  <acceptance_criteria>
    - Phase 4 persistence stores aggregate-friendly summary fields needed by analytics.
    - `src/domain/analytics/query_service.py` can read or reuse persisted realtime evaluation outputs through an explicit helper when needed.
    - The doctor-side response path remains fast and does not trigger a full dashboard recomputation inline.
    - The frontend continues to render persisted `evaluation_id` or equivalent traceable identifier from the backend.
    - `python3 src/domain/analytics/reuse_smoke.py` exits 0 after reading persisted realtime evaluation summaries through the intended analytics helper path.
  </acceptance_criteria>
  <action>Close the Phase 4 loop by bridging persisted realtime evaluation outputs into the analytics model. Extend `src/domain/analytics/contracts.py` and `src/domain/analytics/query_service.py` only as needed to expose persisted realtime evaluation summaries or to keep the write path aligned with existing analytics semantics. Ensure the repository preserves fields analogous to `diagnosis_basis_incomplete`, `missing_diagnosis`, `status`, and optional `quality_index_score`, so later dashboard/drilldown phases can consume them without a second translation layer. Add `src/domain/analytics/reuse_smoke.py` as a focused behavioral verification script that reads persisted Phase 4 rows through the intended analytics helper and exits non-zero if the helper cannot observe the summary fields written by 04-03. Keep the doctor-side request path lightweight: do not run an expensive global analytics refresh inline. If useful, surface the durable `evaluation_id` in `frontend/src/components/doctor-evaluation-panel.tsx` so the doctor-side UI can show a traceable evaluation record reference.</action>
  <verify>
    <automated>python3 -m py_compile src/domain/analytics/contracts.py src/domain/analytics/query_service.py src/domain/analytics/reuse_smoke.py src/api/evaluation/repository.py && python3 src/domain/analytics/reuse_smoke.py</automated>
  </verify>
  <done>EVAL-04 已经闭环：诊鉴结果被存档，并与统计/下钻复用方向保持一致语义。</done>
</task>

</tasks>

<verification>
执行 `export DATABASE_URL='postgresql://postgres:postgres@127.0.0.1:5432/postgres' && PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh eval_01_persistence_chain`，再执行 `python3 -m py_compile src/api/evaluation/repository.py src/api/evaluation/orchestration.py src/api/http_realtime_evaluation.py src/domain/analytics/contracts.py src/domain/analytics/query_service.py src/domain/analytics/reuse_smoke.py`，最后执行 `python3 src/domain/analytics/reuse_smoke.py`。
</verification>

<success_criteria>
`EVAL-04` 所需的持久化记录与 analytics reuse 链路已经被明确计划并具备自动验证路径。
</success_criteria>

<output>
After completion, create `.planning/phases/04-realtime-evaluation-workflow/04-realtime-evaluation-workflow-03-SUMMARY.md`
</output>
