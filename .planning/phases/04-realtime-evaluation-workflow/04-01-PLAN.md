---
phase: 04-realtime-evaluation-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/doctor-evaluation-page.tsx
  - frontend/src/app/__tests__/doctor-evaluation-page.test.tsx
  - frontend/src/components/doctor-evaluation-panel.tsx
  - frontend/src/components/evaluation-result-card.tsx
  - frontend/src/components/evaluation-summary-banner.tsx
  - frontend/src/components/evaluation-suggestion-list.tsx
  - frontend/src/components/__tests__/doctor-evaluation-panel.test.tsx
  - frontend/src/lib/doctor-evaluation-embed.tsx
  - frontend/src/lib/emr-encounter-contracts.ts
  - frontend/src/lib/realtime-evaluation-contracts.ts
autonomous: true
requirements:
  - EVAL-01
  - EVAL-03
must_haves:
  truths:
    - "医生端新增可嵌入式诊鉴面板，明确展示病历上下文并提供单一 `诊鉴` 主操作。"
    - "结果展示结构固定可审查，至少包含诊断依据完整性、潜在缺漏诊断、评估依据和行动建议。"
    - "页面必须包含 loading、success、timeout、error 四类状态容器，不允许只显示裸文本。"
    - "医生看到的是辅助评估结果展示与补充建议，不包含替代临床判断的自主诊断表述。"
  artifacts:
    - path: "frontend/src/app/doctor-evaluation-page.tsx"
      provides: "Doctor-facing evaluation shell component"
      contains: "诊鉴"
    - path: "frontend/src/lib/doctor-evaluation-embed.tsx"
      provides: "Host-facing EMR embedding seam"
      contains: "mountDoctorEvaluationEmbed"
    - path: "frontend/src/lib/emr-encounter-contracts.ts"
      provides: "Typed EMR encounter payload contract"
      contains: "EmrEncounterContext"
  key_links:
    - from: "frontend/src/lib/doctor-evaluation-embed.tsx"
      to: "frontend/src/lib/emr-encounter-contracts.ts"
      via: "EMR host mount seam accepts a typed encounter payload from the medical-record screen"
      pattern: "EmrEncounterContext"
    - from: "frontend/src/lib/doctor-evaluation-embed.tsx"
      to: "frontend/src/app/doctor-evaluation-page.tsx"
      via: "host seam mounts the evaluation shell without router assumptions"
      pattern: "DoctorEvaluationPage"
    - from: "frontend/src/app/doctor-evaluation-page.tsx"
      to: "frontend/src/components/doctor-evaluation-panel.tsx"
      via: "evaluation shell renders the doctor panel inside the host medical-record context"
      pattern: "DoctorEvaluationPanel"
---

<objective>
交付医生端实时诊鉴的前端壳，包括触发入口、结果结构和各类模块状态，为后续后端编排提供稳定交互契约。

Purpose: 先把 EVAL-01 与 EVAL-03 的医生侧界面打稳，使 Phase 4 后续服务编排和持久化都有明确的前端请求/响应边界。
Output: host-facing embed seam、typed EMR/realtime contracts、doctor evaluation shell、trigger/result components、页面级与组件级测试。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-realtime-evaluation-workflow/04-RESEARCH.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/02-dashboard-overview/02-UI-SPEC.md
@门诊诊鉴首页-(全栈).md

<interfaces>
Plan 04-01 must establish a frontend contract shaped like:
```ts
export interface RealtimeEvaluationRequest {
  encounter_id: string;
  case_id: string;
  patient_id: string;
  triggered_by_doctor_id: string;
  encounter_snapshot: {
    chief_complaint?: string;
    history_of_present_illness?: string;
    physical_exam?: string;
    auxiliary_exam?: string;
    diagnoses: Array<{
      disease_code?: string;
      disease_name: string;
      is_primary: boolean;
    }>;
  };
}

export interface RealtimeEvaluationResponse {
  evaluation_id: string;
  status: 'success' | 'timeout' | 'failed';
  elapsed_ms: number;
  assistive_notice: string;
  basis_completeness: {
    verdict: 'complete' | 'incomplete';
    summary: string;
    missing_items: string[];
  };
  potential_missing_diagnoses: Array<{
    disease_name: string;
    confidence_label: 'high' | 'medium' | 'low';
    rationale: string;
  }>;
  rationale: string[];
  suggestions: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish doctor-side contracts and embeddable page shell</name>
  <read_first>
    .planning/phases/04-realtime-evaluation-workflow/04-RESEARCH.md
    .planning/PROJECT.md
    .planning/ROADMAP.md
    frontend/src/app/dashboard-overview-page.tsx
    frontend/src/app/__tests__/dashboard-overview-page.test.tsx
    frontend/src/styles/dashboard.css
  </read_first>
  <files>frontend/src/app/doctor-evaluation-page.tsx, frontend/src/app/__tests__/doctor-evaluation-page.test.tsx, frontend/src/lib/emr-encounter-contracts.ts, frontend/src/lib/doctor-evaluation-embed.tsx, frontend/src/lib/realtime-evaluation-contracts.ts</files>
  <acceptance_criteria>
    - `frontend/src/lib/emr-encounter-contracts.ts` defines the host-supplied encounter payload and doctor identity contract for EMR embedding.
    - `frontend/src/lib/doctor-evaluation-embed.tsx` exposes a host mount seam such as `mountDoctorEvaluationEmbed(...)` that accepts external encounter context and a mount target.
    - `frontend/src/app/doctor-evaluation-page.tsx` renders a doctor-facing shell from supplied encounter context instead of assuming a standalone route.
    - `frontend/src/app/__tests__/doctor-evaluation-page.test.tsx` verifies the shell can mount from host-supplied encounter payload and still exposes the `诊鉴` trigger area.
    - `frontend/src/lib/realtime-evaluation-contracts.ts` defines both request and response contracts.
    - The embed seam and doctor shell make room for encounter snapshot fields and result rendering without breaking the host record-entry flow.
  </acceptance_criteria>
  <action>Implement the doctor-side embedding seam on the existing React/Vite stack. Add `frontend/src/lib/emr-encounter-contracts.ts` with an explicit `EmrEncounterContext`-style contract that models host-supplied encounter sections, diagnoses, patient/doctor identifiers, and any host callback needed to avoid disrupting the medical-record entry flow. Add `frontend/src/lib/realtime-evaluation-contracts.ts` with explicit realtime evaluation request/response interfaces, preserving backend snake_case field names. Build `frontend/src/app/doctor-evaluation-page.tsx` as a host-driven shell component that renders from supplied encounter context, shows a visible primary `诊鉴` action zone, and reserves space for structured assistive results without assuming router ownership. Add `frontend/src/lib/doctor-evaluation-embed.tsx` with a concrete mount helper such as `mountDoctorEvaluationEmbed(container, props)` so the EMR host can inject encounter payload and mount the doctor shell inside the existing medical-record screen. Add `frontend/src/app/__tests__/doctor-evaluation-page.test.tsx` to assert that the shell mounts from host-supplied encounter payload, renders doctor-facing context, and preserves the primary trigger entry.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/app/__tests__/doctor-evaluation-page.test.tsx && npm run build</automated>
  </verify>
  <done>医生端已经具备明确的 EMR 嵌入契约和 mount seam，后续执行编排可以直接接到病历界面内的一键诊鉴入口。</done>
</task>

<task type="auto">
  <name>Task 2: Implement trigger/result components and user-visible module states</name>
  <read_first>
    .planning/phases/04-realtime-evaluation-workflow/04-RESEARCH.md
    frontend/src/app/doctor-evaluation-page.tsx
    frontend/src/components/overview-section.tsx
    frontend/src/components/trend-section.tsx
    frontend/src/lib/realtime-evaluation-contracts.ts
  </read_first>
  <files>frontend/src/components/doctor-evaluation-panel.tsx, frontend/src/components/evaluation-result-card.tsx, frontend/src/components/evaluation-summary-banner.tsx, frontend/src/components/evaluation-suggestion-list.tsx, frontend/src/components/__tests__/doctor-evaluation-panel.test.tsx</files>
  <acceptance_criteria>
    - `frontend/src/components/doctor-evaluation-panel.tsx` accepts an `onRunEvaluation`-style callback boundary instead of hard-wiring backend logic in this plan.
    - The panel exposes loading, success, timeout, and error states with explicit headings/copy.
    - Result rendering includes diagnosis-basis completeness, potential missing diagnoses, rationale, and suggestions.
    - Assistive positioning is visible in the UI and does not read like an autonomous diagnosis.
    - `frontend/src/components/__tests__/doctor-evaluation-panel.test.tsx` verifies trigger interaction, callback disablement while loading, and all major module states.
  </acceptance_criteria>
  <action>Implement the realtime evaluation UI components around the contracts from Task 1. Add `frontend/src/components/doctor-evaluation-panel.tsx` as the state owner for the doctor-facing shell, but keep backend execution behind an injected `onRunEvaluation(request)` callback boundary so this plan remains frontend-only and 04-02 can later wire the real POST client. The panel must disable duplicate submission while loading and render explicit loading, success, timeout, and error containers. Add supporting structured components such as `evaluation-result-card.tsx`, `evaluation-summary-banner.tsx`, and `evaluation-suggestion-list.tsx` so the result view is broken into readable sections rather than a single unstructured block. Ensure the assistive notice is always visible, and keep the rendering deterministic so backend contracts control the content model. Add focused Vitest coverage that verifies initial shell render, in-flight button disablement, successful structured result render, timeout state, and error state through the injected callback boundary.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/__tests__/doctor-evaluation-panel.test.tsx src/app/__tests__/doctor-evaluation-page.test.tsx && npm run build</automated>
  </verify>
  <done>医生端已经具备稳定的触发和结果展示结构，满足 EVAL-01/EVAL-03 的前端边界。</done>
</task>

</tasks>

<verification>
执行 `cd frontend && npm test -- --run src/components/__tests__/doctor-evaluation-panel.test.tsx src/app/__tests__/doctor-evaluation-page.test.tsx && npm run build`。
</verification>

<success_criteria>
`EVAL-01` 与 `EVAL-03` 对应的医生端触发入口和结构化结果展示边界已经可被自动验证。
</success_criteria>

<output>
After completion, create `.planning/phases/04-realtime-evaluation-workflow/04-realtime-evaluation-workflow-01-SUMMARY.md`
</output>
