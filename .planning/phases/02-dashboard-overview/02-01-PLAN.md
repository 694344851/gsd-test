---
phase: 02-dashboard-overview
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/vite.config.ts
  - frontend/vitest.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/styles/dashboard.css
  - frontend/src/app/dashboard-overview-page.tsx
  - frontend/src/components/time-range-toolbar.tsx
  - frontend/src/components/overview-section.tsx
  - frontend/src/components/trend-section.tsx
  - frontend/src/lib/analytics-filters.ts
  - frontend/src/lib/overview-contracts.ts
  - frontend/src/lib/overview-api.ts
  - frontend/src/lib/trend-contracts.ts
  - frontend/src/lib/trend-api.ts
  - frontend/src/test/setup.ts
  - frontend/src/components/__tests__/time-range-toolbar.test.tsx
  - frontend/src/app/__tests__/dashboard-overview-page.test.tsx
  - src/api/http_dashboard_overview.py
  - src/api/http_dashboard_trend.py
  - src/api/http_dashboard_server.py
  - scripts/run_dashboard_api.py
autonomous: true
requirements:
  - DASH-02
must_haves:
  truths:
    - "管理端首页有可运行的 dashboard 壳，且时间范围状态是全页唯一数据源。"
    - "用户能直接点击 10 个快捷时间范围并打开自定义时间输入，而不是先进入自由输入流程。"
    - "页面顶部始终显示绝对起止日期和“截止昨日”的口径说明。"
    - "前端通过明确的 `/api/analytics/overview` 和 `/api/analytics/trend` JSON 边界消费 Phase 1 查询结果，不在浏览器中推导时间桶。"
  artifacts:
    - path: "frontend/src/app/dashboard-overview-page.tsx"
      provides: "Dashboard root page with shared filter state"
      contains: "TimeRangeToolbar"
    - path: "frontend/src/components/time-range-toolbar.tsx"
      provides: "Quick range chips and custom date entry"
      contains: "应用时间范围"
    - path: "frontend/src/lib/analytics-filters.ts"
      provides: "Locked Phase 2 range registry and default filter helpers"
      contains: "last_3_months"
    - path: "src/api/http_dashboard_server.py"
      provides: "HTTP server entry that mounts overview and trend handlers"
      contains: "/api/analytics/overview"
    - path: "scripts/run_dashboard_api.py"
      provides: "CLI to start the local dashboard API bridge"
      contains: "8000"
  key_links:
    - from: "frontend/src/app/dashboard-overview-page.tsx"
      to: "frontend/src/components/time-range-toolbar.tsx"
      via: "single filter state passed as props"
      pattern: "filters=|onFiltersChange="
    - from: "frontend/src/lib/overview-api.ts"
      to: "/api/analytics/overview"
      via: "HTTP GET with range_key/as_of_date query params"
      pattern: "range_key|as_of_date"
    - from: "frontend/src/lib/trend-api.ts"
      to: "/api/analytics/trend"
      via: "HTTP GET with range_key/as_of_date query params"
      pattern: "range_key|as_of_date"
    - from: "src/api/http_dashboard_overview.py"
      to: "src/api/analytics/overview.py"
      via: "handler delegates to build_overview_response"
      pattern: "build_overview_response"
    - from: "src/api/http_dashboard_trend.py"
      to: "src/api/analytics/trend.py"
      via: "handler delegates to build_trend_response"
      pattern: "build_trend_response"
---

<objective>
建立 Phase 2 的最小可运行 dashboard 壳、时间筛选骨架和本地 HTTP 数据桥，给后续概览卡与趋势图提供稳定宿主。

Purpose: 先把前端运行时、测试基座、全页筛选状态和数据入口锁死，避免后续卡片和图表计划分别重复搭壳或改写时间语义。
Output: React + Vite + Vitest 前端骨架、基于 Python wrapper 的 `/api/analytics/*` HTTP 桥、时间筛选组件与页面占位模块。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dashboard-overview/02-CONTEXT.md
@.planning/phases/02-dashboard-overview/02-RESEARCH.md
@.planning/phases/02-dashboard-overview/02-VALIDATION.md
@.planning/phases/02-dashboard-overview/02-UI-SPEC.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-VERIFICATION.md
@门诊诊鉴首页-(全栈).md

<interfaces>
Existing backend wrappers the HTTP bridge must delegate to:

From `src/domain/analytics/contracts.py`:
```python
AnalyticsTimeRangeKey = Literal[
    "last_7_days",
    "last_30_days",
    "last_3_months",
    "last_6_months",
    "last_12_months",
    "previous_week",
    "previous_month",
    "previous_quarter",
    "previous_year",
    "custom",
]

@dataclass(slots=True)
class AnalyticsFilters:
    as_of_date: str
    range_key: AnalyticsTimeRangeKey
    start_date: str | None = None
    end_date: str | None = None
    department_ids: list[str] | None = None
    department_type_ids: list[str] | None = None
    doctor_ids: list[str] | None = None
    disease_ids: list[str] | None = None
```

From `src/api/analytics/overview.py` and `src/api/analytics/trend.py`:
```python
def build_overview_response(filters: AnalyticsFilters, *, executor=None, connection_string=None) -> dict[str, Any]
def build_trend_response(filters: AnalyticsFilters, *, executor=None, connection_string=None) -> dict[str, Any]
```

Plan 01 must create these stable frontend contracts for later plans to implement against:
```ts
export interface DashboardFilters {
  asOfDate: string;
  rangeKey:
    | 'last_3_months'
    | 'last_6_months'
    | 'last_12_months'
    | 'last_30_days'
    | 'last_7_days'
    | 'previous_week'
    | 'previous_month'
    | 'previous_quarter'
    | 'previous_year'
    | 'custom';
  startDate?: string;
  endDate?: string;
}

export interface OverviewSectionProps {
  filters: DashboardFilters;
}

export interface TrendSectionProps {
  filters: DashboardFilters;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the local HTTP bridge and frontend runtime/tooling baseline</name>
  <read_first>
    package.json
    src/domain/analytics/contracts.py
    src/domain/analytics/query_service.py
    src/api/analytics/overview.py
    src/api/analytics/trend.py
    .planning/phases/02-dashboard-overview/02-CONTEXT.md
    .planning/phases/02-dashboard-overview/02-RESEARCH.md
    .planning/phases/02-dashboard-overview/02-VALIDATION.md
  </read_first>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/vite.config.ts, frontend/vitest.config.ts, frontend/index.html, frontend/src/lib/overview-contracts.ts, frontend/src/lib/overview-api.ts, frontend/src/lib/trend-contracts.ts, frontend/src/lib/trend-api.ts, frontend/src/test/setup.ts, src/api/http_dashboard_overview.py, src/api/http_dashboard_trend.py, src/api/http_dashboard_server.py, scripts/run_dashboard_api.py</files>
  <acceptance_criteria>
    - `frontend/package.json` contains scripts named `"dev"`, `"build"`, and `"test"`.
    - `frontend/package.json` includes `react`, `react-dom`, `vite`, `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `echarts`, and `echarts-for-react`.
    - `frontend/vite.config.ts` contains a dev-server proxy from `"/api"` to `"http://127.0.0.1:8000"`.
    - `frontend/vitest.config.ts` contains `environment: 'jsdom'`.
    - `frontend/src/lib/overview-api.ts` contains a fetch path string `'/api/analytics/overview'`.
    - `frontend/src/lib/trend-api.ts` contains a fetch path string `'/api/analytics/trend'`.
    - `frontend/src/lib/overview-api.ts` and `frontend/src/lib/trend-api.ts` both serialize query keys `range_key` and `as_of_date`.
    - `src/api/http_dashboard_overview.py` imports `build_overview_response` and exposes a handler for `"/api/analytics/overview"`.
    - `src/api/http_dashboard_trend.py` imports `build_trend_response` and exposes a handler for `"/api/analytics/trend"`.
    - `src/api/http_dashboard_server.py` contains `ThreadingHTTPServer` or `HTTPServer` and mounts both overview/trend handlers.
    - `scripts/run_dashboard_api.py` contains a default port value `8000`.
    - `frontend/src/lib/overview-contracts.ts` and `frontend/src/lib/trend-contracts.ts` preserve backend snake_case fields such as `outpatient_count`, `success_evaluated_count`, `bucket_label`, and `bucket_grain`.
  </acceptance_criteria>
  <action>Per D-01, D-02, D-03, D-04, D-17 and the Wave 0 requirements in `02-VALIDATION.md`, create a standalone `frontend/` workspace instead of modifying the Phase 1 root `package.json`. In `frontend/package.json`, define scripts exactly as `"dev": "vite"`, `"build": "vite build"`, and `"test": "vitest run"`. Add runtime dependencies `react`, `react-dom`, `echarts`, and `echarts-for-react`; add dev dependencies `typescript`, `vite`, `@vitejs/plugin-react`, `vitest`, `jsdom`, `@testing-library/react`, and `@testing-library/jest-dom`. In `frontend/vite.config.ts`, configure the React plugin and proxy every `/api` request to `http://127.0.0.1:8000`; in `frontend/vitest.config.ts`, set `environment: 'jsdom'`, load `frontend/src/test/setup.ts`, and disable watch mode by using run-only config. Create `frontend/src/lib/overview-contracts.ts` and `frontend/src/lib/trend-contracts.ts` as typed mirrors of the current Python wrapper payloads, preserving backend snake_case keys instead of inventing camelCase view models. Create `frontend/src/lib/overview-api.ts` and `frontend/src/lib/trend-api.ts` that export `loadOverview(filters)` and `loadTrend(filters)` functions, build query strings with exact keys `range_key`, `as_of_date`, `start_date`, `end_date`, `department_ids`, `department_type_ids`, `doctor_ids`, and `disease_ids`, and fetch `/api/analytics/overview` or `/api/analytics/trend` without deriving buckets or ratios in the browser. Create a Python HTTP bridge with four files: `src/api/http_dashboard_overview.py`, `src/api/http_dashboard_trend.py`, `src/api/http_dashboard_server.py`, and `scripts/run_dashboard_api.py`. The overview handler must parse the exact query keys above into `AnalyticsFilters`, delegate to `build_overview_response`, and return JSON with `Content-Type: application/json; charset=utf-8`; the trend handler must do the same via `build_trend_response`. `src/api/http_dashboard_server.py` must mount `GET /api/analytics/overview` and `GET /api/analytics/trend` on a local-only server bound to `127.0.0.1`; `scripts/run_dashboard_api.py` must start that server on port `8000` by default and allow an override through `DASHBOARD_API_PORT`. Do not add Flask, FastAPI, or any other new backend framework in this plan; the bridge must use Python standard-library HTTP primitives so Phase 2 can run without researching a permanent server framework first.</action>
  <verify>
    <automated>rg -n "\"dev\"|\"build\"|\"test\"|echarts-for-react|@testing-library/jest-dom|/api/analytics/overview|/api/analytics/trend|range_key|as_of_date|ThreadingHTTPServer|8000" frontend/package.json frontend/vite.config.ts frontend/vitest.config.ts frontend/src/lib/overview-api.ts frontend/src/lib/trend-api.ts src/api/http_dashboard_overview.py src/api/http_dashboard_trend.py src/api/http_dashboard_server.py scripts/run_dashboard_api.py</automated>
  </verify>
  <done>本地开发已经有统一的前端工作区、测试入口和 Python HTTP 数据桥，后续 UI 模块只需要消费固定 `/api/analytics/*` 边界。</done>
</task>

<task type="auto">
  <name>Task 2: Implement the dashboard page shell, shared filter state, and toolbar skeleton</name>
  <read_first>
    .planning/phases/02-dashboard-overview/02-CONTEXT.md
    .planning/phases/02-dashboard-overview/02-UI-SPEC.md
    门诊诊鉴首页-(全栈).md
    frontend/index.html
    frontend/src/app/dashboard-overview-page.tsx
    frontend/src/components/time-range-toolbar.tsx
    frontend/src/components/overview-section.tsx
    frontend/src/components/trend-section.tsx
    frontend/src/lib/analytics-filters.ts
  </read_first>
  <files>frontend/index.html, frontend/src/main.tsx, frontend/src/styles/dashboard.css, frontend/src/app/dashboard-overview-page.tsx, frontend/src/components/time-range-toolbar.tsx, frontend/src/components/overview-section.tsx, frontend/src/components/trend-section.tsx, frontend/src/lib/analytics-filters.ts, frontend/src/components/__tests__/time-range-toolbar.test.tsx, frontend/src/app/__tests__/dashboard-overview-page.test.tsx</files>
  <acceptance_criteria>
    - `frontend/src/lib/analytics-filters.ts` contains all 10 option labels `近三个月`, `近半年`, `近一年`, `过去30天`, `过去7天`, `上周`, `上个月`, `上个季度`, `上一年`, `自定义时间`.
    - `frontend/src/lib/analytics-filters.ts` sets the default range key to `last_3_months`.
    - `frontend/src/components/time-range-toolbar.tsx` contains the visible button text `应用时间范围`.
    - `frontend/src/components/time-range-toolbar.tsx` contains summary text fragments `截止昨日` and `至`.
    - `frontend/src/components/time-range-toolbar.tsx` uses native date inputs with `type="date"` for custom time.
    - `frontend/src/app/dashboard-overview-page.tsx` renders `TimeRangeToolbar`, `OverviewSection`, and `TrendSection` in that order.
    - `frontend/src/components/overview-section.tsx` exports `OverviewSection` and contains a stable placeholder marker `data-testid="overview-section-placeholder"`.
    - `frontend/src/components/trend-section.tsx` exports `TrendSection` and contains a stable placeholder marker `data-testid="trend-section-placeholder"`.
    - `frontend/src/app/__tests__/dashboard-overview-page.test.tsx` asserts the default `last_3_months` selection and the presence of both section placeholders.
    - `frontend/src/components/__tests__/time-range-toolbar.test.tsx` asserts switching to `过去7天` and applying a custom date range.
  </acceptance_criteria>
  <action>Per D-01 through D-04 and the UI-SPEC interaction contract, create `frontend/src/lib/analytics-filters.ts` as the single source of truth for toolbar options and default filter helpers. Define the exact quick-range registry in this order: `last_3_months/近三个月`, `last_6_months/近半年`, `last_12_months/近一年`, `last_30_days/过去30天`, `last_7_days/过去7天`, `previous_week/上周`, `previous_month/上个月`, `previous_quarter/上个季度`, `previous_year/上一年`, `custom/自定义时间`. Export a `createDefaultDashboardFilters(now?: Date)` helper that computes a local “yesterday” ISO date for `asOfDate`, defaults `rangeKey` to `last_3_months`, and leaves `startDate`/`endDate` undefined until `custom` is applied. Create `frontend/src/main.tsx` and `frontend/index.html` so the page mounts a single `DashboardOverviewPage`; add `frontend/src/styles/dashboard.css` with tokenized spacing, surface, accent, and destructive colors that match `02-UI-SPEC.md` (`#F7F8FA`, `#FFFFFF`, `#2F6BFF`, `#D14343`) and a first-screen layout where the five-card summary band area appears before the trend module. In `frontend/src/components/time-range-toolbar.tsx`, render quick-range chips plus a secondary custom-date entry that expands two native `<input type="date">` controls and a primary CTA labeled exactly `应用时间范围`. The toolbar must always render an absolute-date summary line in the form `统计范围 {startDate} 至 {endDate} · 截止昨日（{asOfDate}）`; for ranges other than `custom`, the displayed `startDate` and `endDate` should come from the loaded overview/trend payload once available, and before data arrives the toolbar should still show the current `asOfDate` cutoff plus the selected label. In `frontend/src/app/dashboard-overview-page.tsx`, own the single filter state and pass it into `TimeRangeToolbar`, `OverviewSection`, and `TrendSection`. Export placeholder section components in `frontend/src/components/overview-section.tsx` and `frontend/src/components/trend-section.tsx` that accept `{ filters }`, render headings, and expose `data-testid="overview-section-placeholder"` / `data-testid="trend-section-placeholder"` so later plans can replace internals without changing the page shell. Add tests that freeze `now = new Date('2026-03-25T09:00:00+08:00')`, assert default `asOfDate === '2026-03-24'`, assert `last_3_months` is selected on first render, assert `过去7天` can be selected directly, and assert applying a custom range with `2026-02-01` to `2026-02-29` updates the page-level filters. Do not add routing, React Router, or per-widget duplicated time state in this plan.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/__tests__/time-range-toolbar.test.tsx src/app/__tests__/dashboard-overview-page.test.tsx</automated>
  </verify>
  <done>页面骨架、统一时间筛选状态和后续模块占位都已具备，Plan 02 和 Plan 03 可以并行在各自 section 文件内落地实现。</done>
</task>

</tasks>

<verification>
先执行 `cd frontend && npm test -- --run src/components/__tests__/time-range-toolbar.test.tsx src/app/__tests__/dashboard-overview-page.test.tsx`，再用 `rg` 检查 `/api/analytics/overview`、`/api/analytics/trend`、10 个时间选项和 `截止昨日` 文案都已出现在对应文件中。
</verification>

<success_criteria>
`DASH-02` 所需的页面宿主、时间范围切换骨架、绝对日期展示和本地数据入口已经就位，后续计划无需再重建 app shell 或重定义时间筛选契约。
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-overview/02-dashboard-overview-01-SUMMARY.md`
</output>
