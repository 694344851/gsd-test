---
phase: 02-dashboard-overview
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - sql/tests/dash_02_overview_cards.sql
  - src/domain/analytics/query_service.py
  - src/api/http_dashboard_overview.py
  - frontend/src/lib/overview-contracts.ts
  - frontend/src/lib/overview-api.ts
  - frontend/src/lib/metric-formatters.ts
  - frontend/src/components/overview-card-grid.tsx
  - frontend/src/components/overview-section.tsx
  - frontend/src/components/__tests__/overview-section.test.tsx
autonomous: true
requirements:
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - "首页首屏固定展示 5 张概览卡，顺序与 PRD 锁定顺序完全一致。"
    - "每张可比卡都能显示本期值、较上期变化、正负符号和 N/A 规则。"
    - "整体诊断质量指数卡保留卡位但不伪造分值，也不展示环比。"
    - "较上期所需的上一周期汇总由服务端提供，前端不自行推导比较窗口。"
  artifacts:
    - path: "frontend/src/components/overview-card-grid.tsx"
      provides: "Five-card summary band with locked order"
      contains: "整体诊断质量指数"
    - path: "frontend/src/lib/metric-formatters.ts"
      provides: "Card display and delta formatting helpers"
      contains: "N/A"
    - path: "src/api/http_dashboard_overview.py"
      provides: "Overview endpoint that includes previous period summary"
      contains: "previous_summary"
    - path: "sql/tests/dash_02_overview_cards.sql"
      provides: "Server-side assertions for comparison payload support"
      contains: "analytics.resolve_time_window"
  key_links:
    - from: "src/domain/analytics/query_service.py"
      to: "analytics.resolve_time_window"
      via: "server-side previous-period window calculation"
      pattern: "resolve_time_window"
    - from: "src/api/http_dashboard_overview.py"
      to: "frontend/src/lib/overview-contracts.ts"
      via: "JSON includes summary plus previous_summary"
      pattern: "previous_summary"
    - from: "frontend/src/components/overview-section.tsx"
      to: "frontend/src/lib/metric-formatters.ts"
      via: "cards render current value and delta text from shared helpers"
      pattern: "format"
---

<objective>
补齐概览卡服务端比较数据和前端五卡展示规则，完成首页首屏摘要带。

Purpose: 解决 `DASH-03` 与 `DASH-04`，把上一周期比较窗口、N/A/无变化规则和质量指数占位从“文档约定”变成自动验证的实现。
Output: 带 `previous_summary` 的 overview payload、共享数值格式化器、五张概览卡组件与相应断言。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dashboard-overview/02-CONTEXT.md
@.planning/phases/02-dashboard-overview/02-RESEARCH.md
@.planning/phases/02-dashboard-overview/02-VALIDATION.md
@.planning/phases/02-dashboard-overview/02-UI-SPEC.md
@.planning/phases/02-dashboard-overview/02-01-PLAN.md
@src/domain/analytics/query_service.py
@src/api/analytics/overview.py

<interfaces>
Plan 01 created these boundaries; keep them stable:

```ts
export interface OverviewSectionProps {
  filters: DashboardFilters;
}
```

Current backend overview wrapper returns:
```python
{
  "filters": serialize_filters(filters),
  "summary": get_overview_summary(filters).__dict__,
}
```

Plan 02 must extend the overview HTTP payload to:
```ts
export interface OverviewResponse {
  filters: {
    as_of_date: string;
    range_key: string;
    start_date: string | null;
    end_date: string | null;
  };
  summary: {
    outpatient_count: number;
    success_evaluated_count: number;
    diagnosis_basis_incomplete_rate_by_success: number | null;
    missing_diagnosis_rate_by_success: number | null;
    range_start_date: string;
    range_end_date: string;
  };
  previous_summary: null | {
    outpatient_count: number;
    success_evaluated_count: number;
    diagnosis_basis_incomplete_rate_by_success: number | null;
    missing_diagnosis_rate_by_success: number | null;
    range_start_date: string;
    range_end_date: string;
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add server-side previous-period overview support and contract assertions</name>
  <read_first>
    .planning/phases/02-dashboard-overview/02-CONTEXT.md
    .planning/phases/01-data-foundation/01-VERIFICATION.md
    src/domain/analytics/contracts.py
    src/domain/analytics/query_service.py
    src/api/analytics/overview.py
    src/api/http_dashboard_overview.py
    sql/migrations/002_phase1_metrics.sql
    sql/tests/dash_01_default_window.sql
  </read_first>
  <files>sql/tests/dash_02_overview_cards.sql, src/domain/analytics/query_service.py, src/api/http_dashboard_overview.py, frontend/src/lib/overview-contracts.ts, frontend/src/lib/overview-api.ts</files>
  <acceptance_criteria>
    - `src/domain/analytics/query_service.py` contains a query against `analytics.resolve_time_window`.
    - `src/domain/analytics/query_service.py` contains the strings `previous_start_date` and `previous_end_date`.
    - `src/api/http_dashboard_overview.py` contains a JSON key `previous_summary`.
    - `frontend/src/lib/overview-contracts.ts` contains `previous_summary`.
    - `frontend/src/lib/overview-api.ts` returns an `OverviewResponse` type that includes `previous_summary`.
    - `sql/tests/dash_02_overview_cards.sql` contains assertions for both `range_start_date` and `previous_summary`.
    - `sql/tests/dash_02_overview_cards.sql` references the locked default window date `2026-03-24`.
  </acceptance_criteria>
  <action>Per D-03, D-04, D-11, D-17 and the research warning against frontend-side comparison math, extend the overview data path so the comparison window is resolved on the server side. In `src/domain/analytics/query_service.py`, add a helper that queries `analytics.resolve_time_window(filters.range_key, filters.as_of_date, filters.start_date, filters.end_date)` to retrieve `range_start_date` and `range_end_date`, computes `window_length_days = (range_end_date - range_start_date) + 1`, then derives `previous_end_date = range_start_date - 1 day` and `previous_start_date = previous_end_date - (window_length_days - 1 days)`. Use those concrete dates to call the existing `get_overview_summary` again with `range_key='custom'`, preserving the original `as_of_date`, `department_ids`, `department_type_ids`, `doctor_ids`, and `disease_ids`. If the current range has no data or the previous range has no data, return `previous_summary: null` rather than fabricating zeros. Update `src/api/http_dashboard_overview.py` so the HTTP response shape becomes `{filters, summary, previous_summary}`. Update `frontend/src/lib/overview-contracts.ts` and `frontend/src/lib/overview-api.ts` to reflect that exact payload. Add `sql/tests/dash_02_overview_cards.sql` as a focused assertion script that uses `as_of_date = DATE '2026-03-24'`, proves the current default window still resolves to the Phase 1 `range_start_date`, and proves the server can resolve a non-empty previous comparable window without mutating Phase 1 SQL semantics or adding browser-derived bucket logic. Do not change `analytics.get_overview_summary` itself in this plan; keep Phase 1 SQL semantics stable and build the comparison contract around them.</action>
  <verify>
    <automated>PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh dash_02_overview_cards && rg -n "previous_summary|previous_start_date|previous_end_date|resolve_time_window|2026-03-24" sql/tests/dash_02_overview_cards.sql src/domain/analytics/query_service.py src/api/http_dashboard_overview.py frontend/src/lib/overview-contracts.ts frontend/src/lib/overview-api.ts</automated>
  </verify>
  <done>概览卡所需的上一周期汇总已经通过服务端提供，前端无需也不能自行推导比较时间窗口。</done>
</task>

<task type="auto">
  <name>Task 2: Implement the five overview cards, formatting rules, and empty/partial states</name>
  <read_first>
    .planning/phases/02-dashboard-overview/02-CONTEXT.md
    .planning/phases/02-dashboard-overview/02-UI-SPEC.md
    frontend/src/components/overview-section.tsx
    frontend/src/lib/overview-contracts.ts
    frontend/src/lib/overview-api.ts
    frontend/src/app/dashboard-overview-page.tsx
  </read_first>
  <files>frontend/src/lib/metric-formatters.ts, frontend/src/components/overview-card-grid.tsx, frontend/src/components/overview-section.tsx, frontend/src/components/__tests__/overview-section.test.tsx</files>
  <acceptance_criteria>
    - `frontend/src/components/overview-card-grid.tsx` contains the labels `门诊量`, `评估病例数`, `诊断依据不完整比例`, `缺失诊断比例`, and `整体诊断质量指数` in that order.
    - `frontend/src/lib/metric-formatters.ts` contains the exact strings `N/A`, `较上期 --`, and `待定义`.
    - `frontend/src/lib/metric-formatters.ts` contains both arrow symbols `↑` and `↓` and both count symbols `+` and `-`.
    - `frontend/src/components/overview-section.tsx` contains the empty-state heading `当前时间范围暂无诊鉴数据`.
    - `frontend/src/components/overview-section.tsx` contains the error-state copy `首页数据加载失败，请刷新页面或稍后重试。`
    - `frontend/src/components/overview-section.tsx` fetches through `loadOverview(` and does not contain direct ratio formulas like `/ 100`, `* 100`, or date-window derivation logic.
    - `frontend/src/components/__tests__/overview-section.test.tsx` asserts `N/A`, `较上期 --`, and `待定义` rendering.
  </acceptance_criteria>
  <action>Per D-05 through D-12, D-18, and D-19, replace the overview placeholder with a complete five-card module. In `frontend/src/lib/metric-formatters.ts`, implement shared helpers with these exact display rules: quantity metrics render as integers with no decimal digits; ratio metrics convert fractional values like `0.1148` into percentage strings with at most two decimal places and trimmed trailing zeros (`11.48%`, `0.5%`); unchanged deltas render exactly `较上期 --`; non-comparable deltas render exactly `N/A`; the quality-index primary value renders exactly `待定义`; and the quality-index delta row is omitted entirely. For count cards (`门诊量`, `评估病例数`), compute `delta = current - previous` and render `较上期 +{abs(delta)}` in red when positive, `较上期 -{abs(delta)}` in green when negative, and `较上期 --` when zero. For ratio cards (`诊断依据不完整比例`, `缺失诊断比例`), compute percentage-point change from the current and previous fractional values, render `较上期 ↑{value}` in red when the current ratio is higher, `较上期 ↓{value}` in green when lower, and `较上期 --` when unchanged. If `previous_summary` is null, or the relevant previous metric is null, or the previous metric is exactly zero, render `N/A` for that card’s delta. Create `frontend/src/components/overview-card-grid.tsx` to render exactly five cards in the locked order from D-05. In `frontend/src/components/overview-section.tsx`, call `loadOverview(filters)`, show the empty state `当前时间范围暂无诊鉴数据` plus body copy `请更换时间范围后重试。` when `summary.outpatient_count === 0` and the key overview counts are all zero, show the error copy from `02-UI-SPEC.md` on fetch failure, and keep partial-data handling local to the missing card instead of collapsing the whole module. Do not add helper paragraphs inside cards, do not expose `triggered_evaluation_count` as a separate homepage card, and do not fabricate a numeric quality-index score.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/components/__tests__/overview-section.test.tsx && rg -n "门诊量|评估病例数|诊断依据不完整比例|缺失诊断比例|整体诊断质量指数|N/A|较上期 --|待定义|当前时间范围暂无诊鉴数据|首页数据加载失败，请刷新页面或稍后重试。" frontend/src/components/overview-card-grid.tsx frontend/src/lib/metric-formatters.ts frontend/src/components/overview-section.tsx</automated>
  </verify>
  <done>首页摘要带已经按锁定顺序和规则展示 5 张卡，且所有环比、N/A、占位、空态与异常态规则都有自动验证。</done>
</task>

</tasks>

<verification>
运行 `PATH="/home/healink/ykhl/test-ai/bin:$PATH" bash sql/tests/run-phase-01.sh dash_02_overview_cards` 验证服务端比较窗口，再执行 `cd frontend && npm test -- --run src/components/__tests__/overview-section.test.tsx` 验证五卡渲染与格式化规则。
</verification>

<success_criteria>
`DASH-03` 与 `DASH-04` 对应的本期值、较上期规则、五卡顺序和质量指数占位都已落成，并且比较窗口语义留在服务端。
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-overview/02-dashboard-overview-02-SUMMARY.md`
</output>
