---
phase: 02-dashboard-overview
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - frontend/src/lib/trend-contracts.ts
  - frontend/src/lib/trend-api.ts
  - frontend/src/lib/chart-options.ts
  - frontend/src/lib/__tests__/chart-options.test.ts
  - frontend/src/components/diagnosis-trend-chart.tsx
  - frontend/src/components/trend-section.tsx
  - frontend/src/components/__tests__/trend-section.test.tsx
autonomous: true
requirements:
  - TREN-01
  - TREN-02
  - TREN-03
must_haves:
  truths:
    - "趋势图能按所选时间范围切换横轴粒度，并直接使用 Phase 1 返回的 bucket_label 与 bucket_grain。"
    - "图上同时显示门诊量、评估病例数、诊断依据不完整比例和缺失诊断比例四个核心指标。"
    - "悬停任意时间点时，tooltip 一次展示时间和四个指标值。"
    - "无数据时显示明确空态，单指标缺失时仅该指标显示不可用。"
  artifacts:
    - path: "frontend/src/lib/chart-options.ts"
      provides: "ECharts dual-axis mixed chart option builder"
      contains: "barGap: '-100%'"
    - path: "frontend/src/components/diagnosis-trend-chart.tsx"
      provides: "React ECharts wrapper"
      contains: "ReactECharts"
    - path: "frontend/src/components/trend-section.tsx"
      provides: "Trend module with loading/error/empty handling"
      contains: "当前时间范围暂无诊鉴数据"
    - path: "frontend/src/lib/__tests__/chart-options.test.ts"
      provides: "Series and tooltip contract assertions"
      contains: "tooltip"
  key_links:
    - from: "frontend/src/components/trend-section.tsx"
      to: "frontend/src/lib/trend-api.ts"
      via: "section loads series for the active filters"
      pattern: "loadTrend"
    - from: "frontend/src/lib/chart-options.ts"
      to: "frontend/src/lib/trend-contracts.ts"
      via: "bucket_label and bucket_grain drive x-axis labels"
      pattern: "bucket_label|bucket_grain"
    - from: "frontend/src/components/diagnosis-trend-chart.tsx"
      to: "frontend/src/lib/chart-options.ts"
      via: "React wrapper renders the built ECharts option"
      pattern: "buildTrendChartOption"
---

<objective>
交付 Phase 2 的双轴趋势图、tooltip 交互和图表级空态/异常态，完成首页分析主图。

Purpose: 解决 `TREN-01`、`TREN-02`、`TREN-03`，让趋势图在不改写 SQL 语义的前提下稳定展示数量与比例的同屏走势。
Output: ECharts option builder、图表 React 包装组件、趋势区段数据加载与交互测试。
</objective>

<execution_context>
@/home/healink/ykhl/test-ai/.codex/get-shit-done/workflows/execute-plan.md
@/home/healink/ykhl/test-ai/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dashboard-overview/02-CONTEXT.md
@.planning/phases/02-dashboard-overview/02-RESEARCH.md
@.planning/phases/02-dashboard-overview/02-VALIDATION.md
@.planning/phases/02-dashboard-overview/02-UI-SPEC.md
@.planning/phases/02-dashboard-overview/02-01-PLAN.md
@src/api/analytics/trend.py
@src/domain/analytics/contracts.py

<interfaces>
Stable backend trend payload already exists via `build_trend_response`:
```python
{
  "filters": serialize_filters(filters),
  "series": [
    {
      "outpatient_count": int,
      "success_evaluated_count": int,
      "diagnosis_basis_incomplete_rate_by_success": float | None,
      "missing_diagnosis_rate_by_success": float | None,
      "bucket_start": str,
      "bucket_end": str,
      "bucket_label": str,
      "bucket_grain": "shift" | "day" | "week" | "month",
    }
  ]
}
```

Plan 03 should preserve the Plan 01 section boundary:
```ts
export interface TrendSectionProps {
  filters: DashboardFilters;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the dual-axis chart option contract and tooltip formatter</name>
  <read_first>
    .planning/phases/02-dashboard-overview/02-CONTEXT.md
    .planning/phases/02-dashboard-overview/02-UI-SPEC.md
    frontend/src/lib/trend-contracts.ts
    frontend/src/lib/trend-api.ts
    frontend/src/components/trend-section.tsx
  </read_first>
  <files>frontend/src/lib/trend-contracts.ts, frontend/src/lib/trend-api.ts, frontend/src/lib/chart-options.ts, frontend/src/lib/__tests__/chart-options.test.ts</files>
  <acceptance_criteria>
    - `frontend/src/lib/chart-options.ts` contains four series names: `门诊量`, `评估病例数`, `诊断依据不完整比例`, and `缺失诊断比例`.
    - `frontend/src/lib/chart-options.ts` contains `barGap: '-100%'`.
    - `frontend/src/lib/chart-options.ts` contains `trigger: 'axis'`.
    - `frontend/src/lib/chart-options.ts` contains a tooltip border color string `#E5E7EB`.
    - `frontend/src/lib/chart-options.ts` reads `bucket_label` directly and does not generate labels from `new Date(` plus manual bucketing logic.
    - `frontend/src/lib/__tests__/chart-options.test.ts` asserts the left and right y-axis assignments and the presence of all tooltip rows.
  </acceptance_criteria>
  <action>Per D-13 through D-17 and the UI-SPEC trend-chart contract, build a pure option builder in `frontend/src/lib/chart-options.ts`. Accept the raw trend payload rows and map them directly into ECharts config without deriving new time buckets: x-axis labels must come from `bucket_label`, and the current grain display must come from `bucket_grain`. Configure a dual-axis mixed chart with the left y-axis for ratio lines and the right y-axis for quantity bars. Define exactly four visible series: `门诊量` as the neutral gray base bar (`#C9CED6`), `评估病例数` as the blue overlay bar (`#2F6BFF`) with `barGap: '-100%'`, `诊断依据不完整比例` as a line, and `缺失诊断比例` as a line. Convert fractional ratio values such as `0.1148` into percent display values by multiplying by 100 only inside chart-formatting helpers; keep null ratio values as `null` so ECharts renders gaps rather than fake zeros. Configure `tooltip.trigger = 'axis'`, a white background, border color `#E5E7EB`, and a formatter that renders the hovered `bucket_label` plus exactly four rows in this order: 门诊量, 评估病例数, 诊断依据不完整比例, 缺失诊断比例. Quantity rows must render integer values; ratio rows must render percent strings with up to two decimals and `--` when the hovered value is null. Update `frontend/src/lib/trend-contracts.ts` and `frontend/src/lib/trend-api.ts` only as needed to support this option builder; do not change the endpoint path or invent a new combined trend payload in this plan.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/__tests__/chart-options.test.ts && rg -n "门诊量|评估病例数|诊断依据不完整比例|缺失诊断比例|barGap: '-100%'|trigger: 'axis'|#E5E7EB|bucket_label" frontend/src/lib/chart-options.ts frontend/src/lib/__tests__/chart-options.test.ts</automated>
  </verify>
  <done>趋势图 option contract 已经固定，后续 React 包装层只需渲染而不必再决定双轴、tooltip 或系列结构。</done>
</task>

<task type="auto">
  <name>Task 2: Implement the trend section, ECharts wrapper, and module states</name>
  <read_first>
    .planning/phases/02-dashboard-overview/02-CONTEXT.md
    .planning/phases/02-dashboard-overview/02-UI-SPEC.md
    frontend/src/components/trend-section.tsx
    frontend/src/lib/chart-options.ts
    frontend/src/lib/trend-api.ts
  </read_first>
  <files>frontend/src/components/diagnosis-trend-chart.tsx, frontend/src/components/trend-section.tsx, frontend/src/components/__tests__/trend-section.test.tsx</files>
  <acceptance_criteria>
    - `frontend/src/components/diagnosis-trend-chart.tsx` imports `ReactECharts`.
    - `frontend/src/components/trend-section.tsx` fetches through `loadTrend(`.
    - `frontend/src/components/trend-section.tsx` contains the empty-state heading `当前时间范围暂无诊鉴数据`.
    - `frontend/src/components/trend-section.tsx` contains the empty-state body `请更换时间范围后重试。`
    - `frontend/src/components/trend-section.tsx` contains the error-state copy `首页数据加载失败，请刷新页面或稍后重试。`
    - `frontend/src/components/trend-section.tsx` does not contain manual bucket derivation code such as `groupByWeek`, `groupByMonth`, or hand-built `AM`/`PM` arrays.
    - `frontend/src/components/__tests__/trend-section.test.tsx` asserts the tooltip-contract builder is used and that empty/error states render.
  </acceptance_criteria>
  <action>Replace the trend placeholder with a real section implementation. In `frontend/src/components/diagnosis-trend-chart.tsx`, wrap `ReactECharts` and accept a prebuilt option from `buildTrendChartOption`; keep sizing responsive for both desktop and mobile and set a minimum chart height of `360px`. In `frontend/src/components/trend-section.tsx`, call `loadTrend(filters)` whenever the shared toolbar filters change, pass the returned rows into the option builder, and render the chart under a section heading. Treat “no data” as `series.length === 0` or all rows having `outpatient_count === 0`; in that case render the locked empty-state heading/body instead of an empty chart canvas. Treat fetch failure as the locked error message from the UI-SPEC. For partial-data handling, keep quantity bars visible even if one or both ratio lines have null values; only the missing line points should disappear and tooltip rows should show `--` for those null values. Do not add legend toggles, single-series simplified mode, alternative chart types, or frontend-side regrouping of rows by week/month/shift; the section must trust the backend `bucket_label` / `bucket_grain` contract from Phase 1. Add tests that mock a successful trend payload, verify the section passes rows into `buildTrendChartOption`, verify the empty state renders when the payload has no rows, and verify the error copy appears on rejected fetches.</action>
  <verify>
    <automated>cd frontend && npm test -- --run src/lib/__tests__/chart-options.test.ts src/components/__tests__/trend-section.test.tsx && npm run build</automated>
  </verify>
  <done>趋势模块已经完成双轴图、tooltip 和模块级状态管理，满足首页趋势分析主图交付要求。</done>
</task>

</tasks>

<verification>
执行 `cd frontend && npm test -- --run src/lib/__tests__/chart-options.test.ts src/components/__tests__/trend-section.test.tsx && npm run build`，确认 option builder、tooltip、空态/异常态和生产构建都通过。
</verification>

<success_criteria>
`TREN-01`、`TREN-02`、`TREN-03` 对应的趋势图粒度切换、四指标同屏展示和 tooltip 交互均已具备自动验证。
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-overview/02-dashboard-overview-03-SUMMARY.md`
</output>
